# FOMOTrade

监控 [fomo.family](https://fomo.family) 上指定用户的**买入 / 卖出 / 观点**，实时推送到 Telegram Bot。

在同类监控的基础上多做几件事：

- 🌱 **首次建仓识别** —— 这个人是第一次碰这个币，还是在加仓
- 👥 **共识计数** —— 你的监控名单里有多少人买过这个币、还有多少人仍持有
- 📥 **筹码分发预警** —— 名单里有 3 个以上的人「**收到**」同一个币（从外部钱包转入，
  不是自己掏钱买），立刻推送。这往往是项目方在分发筹码，跟买入是相反的信号
- 🆕 **币安 Alpha 上新** —— 币安 Alpha 有新代币上架就推送
- 🎯 **pump.fun 指定用户买卖** —— 盯几个 pump.fun 上的人，他们每成交一笔就推一条（签名/时刻/方向/单价/金额都是**真实逐笔成交**，不是两次快照之间的净变化）
- 🔎 **随手查合约** —— `/ca` 看 FOMO 用户怎么评这个币，`/chips` 看筹码分布

> **关于转入/转出**：早期版本写过「这个平台拿不到转账数据」——**那个结论是错的**。
> 当时只探到 `/v2/transfers/with/{userId}`，它的语义是「**我**与该用户之间的转账」
> （对自己调返回 `400 Cannot fetch transfers with self`），确实没用。
> 后来逆向前端 bundle 找到了 `/v2/users/{userId}/transfers` ——
> 对**别人**调 200 OK，返回他与任意第三方的全部转账。筹码分发预警就建在这上面。

> ⚠️ 本项目**只读** FOMO 接口，不做任何交易操作，不触碰资产。
> ⚠️ 仅供个人研究使用。加密货币交易风险极高，本项目不构成任何投资建议。

---

## 推送长什么样

真实抓取的效果（字段全部来自 API，缺失的行会整行消失、绝不显示 `N/A`）：

```
🌱 wwwwwww · 首次建仓 · $UP        🟢 wwwwwww · 加仓 · $UP
💰 买入 $3,600.94                  💰 买入 $2,487.50
📦 持仓 $6,592.28                  📦 持仓 $6,592.28
📊 均价 $0.3854                    📊 均价 $0.3854
💎 市值 $215.47M                   💎 市值 $215.47M
👥 名单内 1/2 人买过 · 1 人仍持有   👥 名单内 1/2 人买过 · 1 人仍持有
🧬 Solana
5Jr9hGmJgxBRjjF8XGcGgQzXUdsbpZNNMpigEv8Wpump
```

「3 人买过 · 2 人仍持有」的差值本身就是信号——有人已经退出了。

CA 独占最后一行且是纯 `<code>`：在手机上**点一下就复制**，不依赖网络。
（Telegram 内置浏览器不走 MTProto 代理，国内点 GMGN 链接大概率白屏，所以链接不能是主路径。）

---

## 第一次用？三步就能跑起来

装好之后，**你需要做的只有三件事**，其余都是自动的：

```
1. 填 .env        ← Telegram Bot Token + 你的 chat_id
2. bot.ps1 --login ← 弹浏览器,你自己登录 FOMO(程序不经手你的密码)
3. bot.ps1 --run   ← 跑起来,然后在 Telegram 里发命令
```

跑起来之后，**先在 Telegram 里发这几条**，就算上手了：

| 发这个 | 会发生什么 |
|---|---|
| `/help` | 列出全部命令 |
| `/top` | 看 FOMO 今日榜单，挑几个人 |
| `/add <handle>` | 把他加进监控（他一有动作你就收到推送） |
| `/hot` | 你监控的这批人最近都在买什么 |

监控名单为空时不会有任何推送 —— **`/add` 几个人是让它开始工作的第一步**。

---

## 快速开始

### 1. 安装依赖

```powershell
poetry install
```

> **已知问题**：如果 poetry 报 `All attempts to connect to pypi.tuna.tsinghua.edu.cn failed`
> 但 pip 访问同一镜像正常，那是 poetry 自身 HTTP 栈的问题（在代理环境下已复现）。
> 用 pip 装即可，效果等价：
>
> ```powershell
> python -m venv .venv
> .\.venv\Scripts\python.exe -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
> ```

装浏览器（登录取 token 用，只需一次）：

```powershell
.\.venv\Scripts\python.exe -m playwright install chromium
```

### 2. 配置

```powershell
Copy-Item .env.example .env
notepad .env
```

至少要填 `FOMO_TELEGRAM_BOT_TOKEN` 和 `FOMO_TELEGRAM_CHAT_ID`。

### 3. 登录 FOMO

```powershell
.\bot.ps1 --login
```

会弹出一个浏览器窗口，**你自己登录**（程序不经手你的密码）。登录态存在
`data/fomo_session.json`，之后自动续期，不用反复登录。

#### Google 登录报「Couldn't sign you in / This browser or app may not be secure」

Google 会拒绝"被自动化控制"的浏览器。它看三条：启动开关 `--enable-automation`、
`navigator.webdriver === true`、以及浏览器本体是不是真实 Chrome。
`--login` 已经把三条都处理了（持久化 profile + 系统真实 Chrome 渠道 + 抹掉 webdriver 标志），
但 Google 的策略随时会变。真被拦了，按这个顺序试：

**方案一（最省事）：换个登录方式。** FOMO 用的 Privy 支持邮箱验证码登录，
不走 Google OAuth 就没有这个问题。在登录弹窗里选邮箱那一项即可。

**方案二：attach 到你自己的 Chrome。** 这是最可靠的 ——
那就是一个普通 Chrome，Google 完全看不出异常。先把 Chrome 全部退出，然后：

```powershell
& "C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9222
```

在这个 Chrome 里登录 fomo.family，然后另开一个终端：

```powershell
.\bot.ps1 --login --cdp http://127.0.0.1:9222
```

**方案三：** 装个 Chrome（如果系统里只有 Edge 或干脆没有）。
没有系统浏览器时会退回 Playwright 打包的 Chromium，那个版本号和指纹都对不上真实 Chrome，
Google 基本必拦。

### 4. 探测 API（重要，第一次必须跑）

```powershell
.\bot.ps1 --probe
```

本项目的 API 字段是从 fomo.family 前端逆向出来的，**需要用真实响应校准一次**。
这条命令会把各端点的原始 JSON dump 到 `data/fomo_probe/`，并打印一张字段核对表。
详见 [tasks/design.md](tasks/design.md) 第十一节。

### 5. 自检 & 试跑

```powershell
.\bot.ps1 --check      # 配置 / 登录态 / TG / DB 连通性自检
.\bot.ps1 --dry-run    # 跑一轮，只打印不推送
```

### 6. 正式运行

```powershell
.\bot.ps1 --run
```

---

## Telegram 命令

在 Bot 里直接发（只有 `.env` 里配的 admin chat 能用）：

| 命令 | 说明 |
|---|---|
| `/add <handle>` | 加入监控。历史基线会在后台建立，期间照常推送、只是不打徽章 |
| `/hot [今日\|3日\|7日]` | **名单买入榜**：这批人最近在买哪些币，按买入人数排序 |
| `/following <handle>` | 把这个人关注的所有人**批量**加入监控 |
| `/top [周期] [条数]` | 榜单。周期可填 `24h`/`7d`/`30d`/`following`（也认「今日/周/月/关注」），默认今日前 15 |
| `/del <handle>` | 移除监控（软删除，历史数据保留） |
| `/list` | 查看名单和各人的基线状态 |
| `/status` | 运行状态：名单人数、今日事件数、最近一次轮询时间 |
| `/who <CA>` | 名单里谁**买过**这个币（查本地库，秒回） |
| `/ca <CA> [链]` | 查合约：FOMO 上谁在聊这个币、押了多少、赚没赚 |
| `/chips <CA> [链]` | 筹码分布：FOMO 平台与你的名单各持有多少占比 |
| `/copy` | 跟单参数（默认**纸上跟单，不花钱**）；`/copy on` 开启 |
| `/paper` | 跟单台账：每一单现在赚亏多少 |
| `/star <handle>` | 特别关注：他的推送带 ⭐、币名加【】 |
| `/unstar <handle>` | 取消特别关注 |
| `/tin [handle]` | 指定用户的**转入逐条推送**开关（再发一次即关）；不带参数看当前开着谁 |
| `/pump [add\|del <名字或钱包>]` | **pump.fun** 买卖监控名单；不带参数看名单与门槛 |
| `/rebuild` | 重建全部历史基线（回填逻辑改动后用） |
| `/help` | 命令说明 |

在输入框敲 `/` 会弹出命令菜单（启动时自动向 Telegram 注册）。

> 新 `/add` 一个人**不会推送他的历史交易**——游标在加入的瞬间就设成了当前时刻。

`/top` 的每一行都会标注这个人在不在你的监控名单里：

```
🏆 FOMO 榜单 · 24 小时
 1. 👁 change @change            📈 $203.88K · 734 笔
 2. 　 Vee @theveeman            📈 $109.00K · 472 笔
 4. ⏳ J777Crypto📿 @J777Crypto   📈 $99.53K · 183 笔

👁 已监控且基线就绪 · ⏳ 基线建立中 · 无标记 = 还没盯(13 人)
```

榜单的用处不只是看谁在赚——**没标记的就是「排在前面但你还没盯」的人**，直接 `/add` 即可。

### `/hot` 名单买入榜

`/top` 看的是**人**，`/hot` 看的是**币**——你监控的这批人最近在买什么：

```
🔥 名单买入榜 · 近 24 小时
1. $CETS · 👥 3 人买入(12 笔)
   💰 $65.85K · 💎 $2.44M · 🚀 3.2x
   👤 @frankdegods @stigstigstig_ @zinceth
   🧬 BNB Chain · 5 小时前开始买
   0xb0c2ab5af4028461ace3f6e1c33a4ee1404e7777
```

- **名次由买入人数决定**，不是金额——单人反复加仓刷不上来，多个人同时买才是共识
- `🚀 3.2x` = 现在市值 ÷ **名单最早那笔买入时的市值**，也就是「名单开始买之后涨了多少」
- 计价币（USDC/SOL 等）被排除，否则稳定币互换会让 `$USDC` 恒居榜首
- 全部走本地库，零 API 调用，秒回

> **3 日 / 7 日的数据要靠 bot 持续运行积累。** 每轮只拉每人最近 50 笔 swaps，
> 刚启动时更长的窗口和「今日」看着会差不多，跑几天就拉开了。

### `/ca` 查合约 —— 别人怎么看这个币

在推特上看到一个合约地址，直接发给 bot：

```
/ca 5Jr9hGmJgxBRjjF8XGcGgQzXUdsbpZNNMpigEv8Wpump
```

```
$MUSHU · Solana
📋 取到 30 条观点 · 16 位作者(只取最近 30 条,实际可能更多)
@change · $51.47K · 未实现 +$16.86K (+48.7%) · 已实现 +$35.90K (+46.3%)
  keep getting asked what my plans with Mushu are…
@DukerzBreadz · $21.90K · 未实现 +$14.48K (+195.2%) · 已实现 +$990.47 (+10.7%)
…按投入本金排序,还有 8 位未显示

👥 你的名单:无人持有
```

**任意合约都能查**，不限于你名单碰过的币。不指定链会自动依次试。

排序按**投入本金**而不是当前持仓——已经清仓的人本金不是 0，用剩余持仓排会把他们
永远钉在队尾，而「有人在这个币上落袋跑了」恰恰是最该看见的信息。

### `/chips` 筹码分布

```
/chips Ai66LHZG9MCzg1WKdawwqduVAXpNDUuV8M3uyq5ppump
```

```
$CATE · Solana
🏦 FOMO 平台 · 持有人 79,777
   持仓 ≥ 18.0%   ⚠️ 仅统计前 98 名,真实值更高
👥 你的名单 · 7 人在前 98 名内 · ≥7.799%
   @PoorGoat_ · 27,072,573 枚 · $1.04M
   @Aurelius0121 · 13,613,017 枚 · $525.23K
```

⚠️ **注意那个 `≥`**：FOMO 的持有人榜被服务端**硬钳在 100 条**（`limit=1000` 也是 100，
且没有分页），所以持有人超过 100 的币**算不出精确占比，只能给下界**。

持有人 ≤ 100 时才是精确值，那时不带 `≥`：

```
$FOREST · Solana
🏦 FOMO 平台 · 持有人 20 · 持仓 2.815%
👥 你的名单 · 无人持有
```

同类工具在热门币上**不会告诉你这件事**——照样把「前 98 名的合计」写成「持仓占比」。

### 📥 筹码分发预警（自动推送）

名单里有 **3 个以上的人在 24 小时内「收到」同一个币**（从外部钱包转入，不是在 FOMO 上买）
就推送。真实案例：

```
🚨 筹码分发预警 · 3 人「收到」同一个币 · $fih
📥 不是在 FOMO 上买的 —— 币是从外部钱包转进来的
👥 最近 24 小时内 3 人收到 · 合计 $4,252.51
👤 unipcs · $901.37 · ⏱ 5 小时 1 分前
👤 Quanterty · $912.05 · ⏱ 4 小时 58 分前
👤 PoorGoat_ · $2,439.09 · ⏱ 4 小时 56 分前
📮 其中 3 人的币来自同一个发货地址 · 8FtY7n…cZx72 · 前后 5 分 23 秒
💰 名单里另有 1 人真金白银买过:CryptoTalkMan
```

**「5 分 23 秒内、同一个发货地址、发给 3 个人」是可证的事实**，比任何形容词都有力。
消息里刻意不说「他们一分钱没花」——报文里只有钱包地址、没有 userId，
分不出「项目方在分发」和「本人把别处买的币充进来」，替别人断言意图就是编。

阈值/窗口/金额下限：`FOMO_TRANSFER_ALERT_RECEIVERS`（默认 3）、
`FOMO_TRANSFER_ALERT_WINDOW_HOURS`（24）、`FOMO_TRANSFER_ALERT_MIN_USD`（500）。

调阈值前先看频率（本地库 19 天真实数据回放，金额门槛 $500）：

| 阈值 | 频率 |
|---|---|
| 2 人 | 约 5.0 条/天 |
| 3 人（默认） | 约 2.8 条/天 |
| 5 人 | 约 1.4 条/天 |

金额门槛的影响更大：不设门槛 **138.8 条/天**（会被直接静音）、$100 → 10.1、
$500 → 6.0、$1000 → 4.0。噪音几乎全是空投灰尘和平台级批量发放
（美股代币化那类，单个币能有 41 人收到）。再往上会把真信号一起筛掉——
实测最低的那笔真信号是 $901.37。

> **同一个币只报一次，永远。** 触发后哪怕收到的人从 3 个涨到 15 个也不再推。
> 这是刻意的产品决定，不是漏洞（见 `src/store.py` 里 `transfer_in_signals` 的建表注释）。

### 📥 指定用户的转入逐条推送（`/tin`）

有些人的成交发生在**别处**，FOMO 这边看到的是一条「转入」而不是「买入」。
对这些人来说，币到账那一刻才是他动手的那一刻——而上面那条聚合告警是**多人**信号，
问的是「这个币在被分发给一群人」，回答不了「**这一个人**又进货了没有」。

```
/tin unipcs      # 开（再发一次就是关）
/tin             # 看当前开着谁、门槛是多少
```

被点名的人每收到一笔 ≥ `FOMO_TRANSFER_WATCH_MIN_USD`（默认 **$100**）的币，就单独推一条：

```
📥 Frogman (@frogmanhaha) · 收到转入 · $SNDK
💰 数量 16,077,447.32 ≈ $1,018.97
💎 收到时市值 $96.35K
📮 发货地址 0x12d7…ccefb
⏱ 31 秒前到账
🧬 Robinhood Chain
🔗 FOMO
0x2bbd7874fe57cedcb92d71fc13fe9f1ba41877d9
```

> **消息里只有可证的事实。** 报文里只有 fromAddress/toAddress，
> 没有任何东西能说明这笔转账来自哪个工具、是不是买入、花没花钱——
> 所以这条消息不写「他买入了」「他在 XX 上建仓」，只写谁、什么币、多少、
> 收到时市值、从哪个地址来、多久之前。你知道这个人用什么工具，你读得出来。
> 拿不到「收到时市值」那一行就整行消失，绝不用**现在**的市值冒充。

> **「收到时市值」有新鲜度门。** 填进去的是**本轮观测到的**市值，只有转账刚发生时
> 它才等于「收到时」的值。所以到账超过 **120 秒**才渲染的那一条，这一行整行消失——
> 标签不能比数据强，宁可少一格也不换一句更弱的说法把这个数留下。
> （依据：每轮都拉的那条流实测滞后 p50 29s / p90 98s，120s 覆盖约 91.5%。）

- **只对被点名的人生效。** 没开的人一切照旧（落库、进聚合告警，但不逐条推）——
  全名单打开实测约 **807 条/天**（人均 8.49 条），会把买卖推送整个淹掉。
- **同一笔转入既推给你、也照常计入聚合告警的人数**，两个信号回答不同的问题，不去重。
- **只推转入，不推转出。** 提币与「他又进货了」是相反的事。
- **被点名的人每轮都额外再拉一次转账**：只靠轮转要等一圈 20 轮，对「他刚进货」太慢。
  ⚠️ 他**仍然留在轮转池里**（摘出去会让取模基数随开关变化，那正是「有人永远排不上」
  的成因），只是每轮多拉一次而已。
  代价是每轮多 K 个请求（K = 点名人数），所以**最多同时开 12 人**
  （= 默认并发度，恰好多一整波；超了会退回轮转并告警）。
  98 人名单本地模拟：单轮请求峰值 111 → 123（10 波 → 11 波，约 +1.2s）。
- **`/del` 会一并把这个开关关掉**，`/add` 加回来不会自动恢复，要重新 `/tin`。
  它不像 ⭐ 那样只是好看——这一位会真的多花请求、多发消息，静默恢复既会绕过
  12 人上限，也等于在用户没下指令的情况下改了采集频率。
- 推送**失败不会丢**：TG 确认收到之后才标已发，失败的进补发队列下一轮重试。

频率参考：

| 门槛 | 人均频率 | 最活跃的 @unipcs |
|---|---|---|
| 不设门槛 | 8.49 条/天 | 29.22 条/天 |
| $100（默认） | 1.10 条/天 | 3.93 条/天 |
| $500 | 0.73 条/天 | 2.72 条/天 |

（本地库 19.1 天 · 95 个有转入记录的人 · 15,410 条 TRANSFER_IN，2026-08-30 复核。
@unipcs 最大的一笔是 $287,923.72。）

### 🆕 币安 Alpha 上新（自动推送）

币安 Alpha 有新代币上架就推送，命中已知板块时多一行标注：

```
🆕 币安 Alpha 新上架 · $牛来
🏷 板块 · 股票 Meme 幣
💎 市值 $44.16M
👥 持有人 48,873
🕐 上架 2026-08-18 08:20 UTC · 8D前
🧬 BNB Chain
```

- 走**独立的定时任务**（默认 5 分钟一轮），不占用 FOMO 轮询的时间预算；
  币安接口挂掉**不会**影响 FOMO 推送
- **冷启动静默播种**：第一次跑会把当前 665 个代币全部记为已知，一条都不推
- 板块只作为**标注**。板块是币安运营编排的、无文档、随时可能下线，
  绑死在板块上会让功能一改就废；底层盯的是 Alpha 全量名单的差集
- 开关 `FOMO_ALPHA_ENABLED`（默认 `true`）、间隔 `FOMO_ALPHA_INTERVAL_SEC`（300）、
  板块 `FOMO_ALPHA_SECTORS`（`rankType:tabId:显示名`，逗号分隔）

> 上新频率大约 **3~4 天一条**，很久没动静是正常的，不是坏了。

### 🎯 pump.fun 指定用户买卖（`/pump`）

盯住 pump.fun 上的某几个人，他们每成交一笔就推一条：

```
🟩 pump.fun · 买入 · 1000XCryptoD · $QQQB
💰 金额 $1.46
📊 单价 $718.01
⏱ 2026-08-30 09:20 UTC · 17H前
🧬 BNB Chain
🧾 0xc0d4fb38e001b8848d3a5aea213ac7b3ea9feb931e9bd1e5273cee888b7b75eb
🔗 FOMO · GMGN
0x205812cdbed920aff76c6580abd681a46d11efc7
```

```bash
/pump                        # 看名单、门槛、巡检间隔
/pump add hexiecs            # 名字或钱包地址都行
/pump del hexiecs            # 软删除
```

- **两段式**:先用持仓页发现「哪个币变了」，再去逐笔成交接口拿**真实成交**。
  所以文案里的「买入 / 卖出」是报文里的原话，不是我们从净变化里猜的
- 走**独立的定时任务**（默认 60 秒一轮），pump.fun 挂掉**不会**影响 FOMO 推送
- **冷启动静默播种**：每个人被加进来的第一轮只把当前持仓记为已知，一条都不推
  （有人持仓多达 1905 个，不播种就是把 Telegram 打爆）
- **多链**：Solana / BNB Chain / Base / Robinhood / Monad / Hyperliquid 等，
  链名照标；GMGN 不支持的链只丢链接那一行，其余照推
- 请求预算：盯 K 个人 ≈ 每轮 `K + 变动的币数` 个请求（变动通常是 0）
- 开关 `FOMO_PUMP_ENABLED`（**默认 `false`**，要自己打开）、
  间隔 `FOMO_PUMP_INTERVAL_SEC`（60）、门槛 `FOMO_PUMP_MIN_USD`（50）、
  单轮上限 `FOMO_PUMP_MAX_MINTS`（8）、新鲜窗口 `FOMO_PUMP_TRADE_MAX_AGE_SEC`（7200）

> ⚠️ 这条链路**只推通知，永远不下单**，与 `/copy` 那套跟单参数没有任何交集。
> 用的全是 pump.fun 公开免登录接口：不登录、不带任何凭据、不碰交易接口。

### 晚上关机怎么办

如果你像我一样拿自己的电脑跑、睡觉时关机，**不用做任何事**——开机后会自动处理：

```
🌙 停机期间汇总 · 8.0 小时
共 256 条:124 买 · 129 卖 · 3 条观点
已全部入库,不逐条推送(避免几百条过期消息刷屏)。

买入最集中的:
1. $CETS · 👥 2 人 · $58.39K
2. $GENTLE · 👥 2 人 · $22.69K
3. $sami (base) · 👥 2 人 · $18.39K

完整榜单:/hot 今日  ·  恢复正常推送 ✅
```

停机期间的事件**照常入库**——共识计数、首次建仓判定、`/hot` 榜单读的都是库，
数据完整性完全不受影响，只是不拿几百条过期消息轰你。下一轮自动恢复正常推送。

阈值由 `FOMO_CATCHUP_THRESHOLD_MIN` 控制（默认 45 分钟，设 0 关闭）。
45 分钟比正常轮询间隔大两个量级，不会被网络抖动误触发；又足够短，睡一觉起来必然命中。

> ⚠️ **停机超过约一天**时，最活跃的那几个人可能会丢数据：每轮只拉每人最近 50 笔 swaps，
> 而实测最活跃的用户一天就有 49 笔。要长期不间断跑还是得上 VPS。

### 名单规模与轮询间隔

每个被监控用户每轮要拉 swaps + balances + trades，转账另按轮转分摊（一圈约 5 分钟）。
拉取是并发的（`FOMO_FETCH_WORKERS`，默认 **12** 线程；实测 24 是拐点，36 反而更慢）。

| 名单人数 | 单轮实测耗时 | 建议 `FOMO_POLL_INTERVAL_SEC` |
|---|---|---|
| ≤ 20 | < 10s | 20 |
| 68 | ~21s | 35 |
| 96（当前） | 5~18s（稳态） | **27（默认）** |

**间隔一定要留余量。** 上游抖动时单轮会涨到几十秒，间隔贴着耗时设就会一直刷
`skipped ... maximum number of running instances`——数据不会丢（`coalesce` 已开），
但日志会被刷满、推送延迟增加。日志里出现「单轮耗时 Ns > 轮询间隔」时，
它会直接告诉你该调到多少。

`/following` 导入后会直接告诉你当前的预估耗时，超过间隔时会提示该调到多少。
**间隔不够会让 tick 持续堆积**（APScheduler 的 `coalesce` 会合并掉堆积的触发，
表现为实际间隔被拉长、推送延迟增加）。

`/following` 是**一次性导入**，不是持续同步——对方之后新关注的人不会自动进来。
做成持续同步会带来「他取关了要不要自动移除」这种没有正确答案的问题，
而误删会连带把本地已建好的基线一起作废。

---

## 设计文档

完整设计见 [tasks/design.md](tasks/design.md)，包含：

- 逆向得到的 FOMO API 端点与鉴权方式
- 「首次买入」和「共识计数」的精确语义与取舍理由
- 完整 SQLite DDL 与判定逻辑
- 边界情况处置表（拆单、多链同名币、冷启动、动态增删名单…）
- 消息模板与排版铁律
- 17 项待验证的 API 字段清单

实现进度见 [tasks/todo.md](tasks/todo.md)。

---

## 开发

```powershell
.\.venv\Scripts\python.exe -m pytest -q      # 跑测试
.\.venv\Scripts\python.exe -m ruff check .   # 静态检查
```

测试不碰网络、不碰真实数据库，全部用内存 SQLite。

---

## 安全

- 登录环节由你自己在浏览器里完成，**程序不经手密码**
- `data/fomo_session.json` 含 Privy refresh token，已在 `.gitignore` 里；**泄漏等于账号被接管**
- 日志里的 token 一律脱敏
- 本项目不发起任何交易、转账、授权操作
- `.env` 里有 Telegram Bot Token，**不要对它跑 `cat` / `diff` / `head`**——
  要改就 `grep -n <键名>` 定点看、定点改。（这条是踩过坑写的：`sed -i` 顺带
  改了行尾，`diff` 于是判定全文都变，把整个 `.env` 连同 token 一起打了出来。）
- 拉总供应量走的是 `/public/proxy/filterTokens`，**不带鉴权**，
  所以不占用监控进程的会话；但它需要 curl_cffi 的 Chrome 指纹才能过 Cloudflare
  （裸 urllib 会收到 `HTTP 430`）
