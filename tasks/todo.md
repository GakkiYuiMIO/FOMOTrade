# FOMOTrade 实现计划与进度

> 设计文档：[design.md](design.md)（唯一权威规格）
> 本文件只记录**做到哪了**和**下一步做什么**。

---

## 阶段划分

| Phase | 内容 | 出口条件 | 状态 |
|---|---|---|---|
| **0** | `--login` + `--probe`：拿 token、dump 端点、核对 17 项字段清单 | 17 项全部打勾；定下走 `HttpFomoClient` 还是 `PlaywrightFomoClient` | ⏳ **需要用户操作** |
| **1** | 配置 / 数据结构 / 建表 / 归一化纯函数 | 建表成功；纯函数单测通过 | ✅ 完成 |
| **2** | client + poller 最小闭环：拉取 → 归一化 → 去重落库 → `--dry-run` | 跑 10 分钟无重复无崩溃，落库数据肉眼核对正确 | ⏳ 待 Phase 0 |
| **3** | formatter + TG 推送打通，五类事件全接 | 七种消息模板在真实 TG 里渲染正确 | ⏳ 待 Phase 0 |
| **4** | bot 命令层 + 基线建立 | `/add` `/del` `/list` `/status` 可用；验收用例 1/2/9 通过 | ⏳ 待 Phase 0 |
| **5** | 功能 A/B：徽章 + 共识计数 | 验收用例 3~8 全部通过 | ✅ 逻辑完成，待真实数据验证 |
| **6** | 长跑稳定性：token 续期、429 退避、`sent` 补发、异常告警 | 连续跑 24h 无人工干预 | ⏳ 待 Phase 0 |

---

## 已完成

- [x] 项目骨架：`.gitignore`（先于 `git init` 落地，杜绝 `.env`/session 有任何一瞬间可提交）、`pyproject.toml`、`requirements.txt`、`.env.example`、`README.md`
- [x] `src/config.py` —— `FomoSettings` 独立 `BaseSettings`；路径常量；`mask()` 脱敏
- [x] `src/logger.py` —— loguru 初始化；脱敏正则**补了 `_-` 字符类**（原 claudeTrade 版本会漏掉 Privy 的 JWT 和 refresh token）
- [x] `src/notifier.py` —— `send()` + `get_updates()`；429 按 `retry_after` 退避；4000 字符截断保护
- [x] `src/models.py` —— `FomoEvent`、计价币白名单、归一化纯函数
- [x] `src/store.py` —— 4 张表、`tx()` 显式事务、功能 A/B 全部判定逻辑
- [x] `src/auth.py` —— Privy 登录态获取 + refresh token 自动续期
- [x] `src/client.py` —— `FomoClient` 协议 + `HttpFomoClient`(curl_cffi) + `PlaywrightFomoClient`
- [x] `src/formatter.py` —— 七种场景消息模板
- [x] `src/poller.py` —— 两阶段 tick、seeding、共识计算、归一化
- [x] `src/bot.py` / `src/cli.py` —— TG 命令层与 CLI 入口
- [x] `tests/` —— **204 项测试全绿（0.89s）**，`ruff check` 全通过

### 集成评审修复（并行开发后的契约漂移，逐条已修）

| # | 问题 | 后果 | 修法 |
|---|---|---|---|
| 2.1 | `AuthError` 被 `_fetch_snapshots` 的裸 `except` 吞掉 | token 过期后每 20s 空转刷日志，§3.5 要求的「🔐 登录态失效」TG 告警**永远发不出去**，用户以为监控还活着 | 加 `except AuthError: raise`；`seed_next_pending_user` 同理 |
| 2.2 | 方向判不出的转账被渲染成「📥 收到转入」 | 一笔实际是转出的记录显示成收到转入，**彻底的错误信息** | `_title_anchor` 前置 `side_unknown` 门 → 标题写「交易」；对手方改中性「对手方」；不再无条件加「转账获得」 |
| 2.3 | `load_unsent_recent` 的「10 分钟窗口」实际是「同一 UTC 日全部」 | 一条发不出去的消息被每 20s 重试一整天，积到几百条时单 tick 跑几十分钟，**正常推送被饿死** | 下界改用 `models.iso_minutes_ago()`，与 `now_iso()` 同一生产者 |
| 2.4 | `--dry-run` 会真发 TG，且该消息未转义 | 名不副实；昵称含一个裸 `<` 就 400 | `seed_next_pending_user(dry_run)` 透传 + `html.escape` |
| 2.5 | `Poller.run_forever` 与 `cli.cmd_run` 各一套调度器 | 前者是死代码且不回填 `last_tick_at`，谁改用它 `/status` 会静默失准 | 删掉 `Poller.run_forever`，调度只留 cli 一个入口 |
| 2.6 | 时间戳解析失败时静默兜底成 now | 字段名/单位判错 → 全部越过游标 → 首个 tick 把几百条历史轰进 TG，**日志里一个字都没有** | 新增 `_guard_ts` 批级检查：>50% 失败则丢批 + ERROR |
| 三.2 | `raw_get` 丢掉响应头 | probe #15/#16 永远只能标「无法验证」，Phase 0 打不满勾 | 返回值改三元组，probe 表实际检查限流头 |
| 三.3 | 「不允许边落库边推送」无任何测试保护 | 谁顺手改一行，单测全绿但线上共识数自相矛盾 | 新增 `tests/test_poller.py`（14 条） |
| 三.5 | `cli.py` 导入 `models._NETWORK_ALIASES` 私有名 | 改名即 ImportError | 加公开的 `known_networks()` |

> 评审同时确认：**无致命问题**，无循环导入，被冻结的 5 个契约文件未被改动，
> `judge_badge` 在 `upsert_stats` 之前、`upsert_stats` 只在真插入时调用、
> `mark_sent` 只在 `send()` 成功后调用 —— 三条关键时序全部正确。

---

---

## Phase 0 实测结论（2026-08-11，已完成）

`--login` + `--probe` 已跑通，设计文档 §11 的字段假设被真实数据大幅修正：

### 根因级发现

| 发现 | 影响 |
|---|---|
| **`X-Supported-Chains` 必须是数字链 ID**（`1,56,143,4663,8453,1399811149`），不是链名 | 写错时服务端**不报错、直接把所有结果过滤成空数组**：swaps / trades / watchlist 全返回 0 条且 HTTP 200。这一个头就让整个项目看起来"能连通但没数据" |
| **响应统一信封 `responseObject`** | 不剥离则 `get_swaps()` 恒返回 0，而 `raw_get()` 同一请求能拿到 100 条 |
| **swap 是双侧扁平记录** `inTokenAddress` / `outTokenAddress` / `inHumanAmount` / `outHumanAmount` | 原按嵌套对象取，两侧都取不到 → 所有买卖降级成 `side_unknown` |
| **balances 顶层只有 `balance` / `tokenFilterResult` / `userToken` / `activeTrade`** | `_balance_key` 原按扁平结构取，恒返回 `(None, None)` → `count_holders` 一个都数不到、seeding 持仓回填整个失效 |

### 字段清单核对结果

- ✅ `#1 networkId`：有，**数字**（`1399811149` = Solana）。`swap` 顶层有，分腿还有 `inNetworkId`/`outNetworkId`——**存在跨链 swap**，聚合键必须用分腿的
- ✅ `#2 稳定 id`：`id` 字段稳定。**没有 txHash** → 兜底 hash 用不上，走原生 id 路径
- ✅ `#3 时间戳`：`createdAt` 是 ISO 字符串（`2026-08-11T05:55:04.720Z`）
- ✅ `#4 方向`：无显式 side 字段，靠**计价币位置**判定（设计 §6 的建模澄清正确）
- ✅ `#5 分页`：`tokenAddress`（按币过滤）+ `lastSwapIdV2`（游标），**不是 offset**
- ✅ `#9 均价`：`userToken.averageEntryPriceUsd`；成本 `currentCostBasisUsd`
- ✅ `#13 计价币`：白名单命中，无需补充
- ✅ `#17 Cloudflare`：带 Bearer 后全部放行 → **走轻量 `HttpFomoClient`**，不需要 Playwright 常驻
- ❌ `#8 交易次数`：swap 记录里没有 → **Q3 的否决票无从启用**，`judge_badge` 里那段注释保持注释
- ❌ `#11 转账`：端点语义是「我与该用户之间」→ **转入/转出砍掉**
- ⚠️ `#12 观点`：**没有按用户查的端点**（`/feed/user/thesis` 是 404）。改为「遍历持仓币 → 按币拉 `/feed/token/thesis` → 按 userId 过滤」，`afterTime` 单位是**毫秒**

### 端到端验证

真实用户真实交易跑通：🌱 首次建仓 / 🟢 加仓 / 🔴 卖出 三种徽章全判对，
同一个币第二次买入正确从 FIRST 变 ADD，共识计数与持仓/均价/市值全部正常。

### 真实运行后补充发现（用户实测自己的账号）

| 发现 | 症状 | 处置 |
|---|---|---|
| **`/feed/token/thesis` 必须显式传 `threshold=0`** | 观点功能完全不工作，且**无任何报错**。该参数按发帖人的**持仓美元额**过滤，不传时服务端用非 0 默认值 —— 响应里那个 `"threshold": 3.714...` 正好等于 `authorTrade.usdValue`。实测同一个币：不传 → 100 条里 0 条是目标用户的；传 `threshold=0` → 有 2 条。**大仓位能抓到、小仓位抓不到**，这种部分生效比全挂更难发现 | 固定传 `threshold=0` |
| 稳定币互换被渲染成「🟢 加仓 $USDC」 | USDT→USDC 兑换既不是建仓也不是离场，纯占屏 | `quote_only` 标志：落库但不推送。**落库时立即 `mark_sent`**，否则补发队列会每 20 秒捞一次、连捞 10 分钟 |

> 三个「HTTP 200、不报错、结果被静默过滤」的坑（`X-Supported-Chains` 用链名、
> 响应信封没剥离、thesis 漏传 `threshold`）合计吃掉了大部分排查时间。
> **教训：这个 API 对不认识的参数一律静默降级，不返回错误。**
> 以后遇到「能连通但没数据」，第一步就该用已登录的浏览器 profile 抓 App 自己发的请求做逐字段对照，
> 而不是逐个猜参数。

### 已知盲区

- **观点只能看到"监控用户当前持仓的币"下的**。他对已清仓的币发的观点看不到 —— 这是按币查的必然结果
- 链 `4663` 没有可读名，显示为数字

---

## 下一步（需要用户操作）

Phase 0 是硬门槛，**必须由用户本人完成登录**（程序绝不代填密码）：

```powershell
.\bot.ps1 --login      # 弹浏览器，自己登录 FOMO
.\bot.ps1 --probe      # dump 端点原始 JSON + 打印字段核对表
```

probe 结果决定三件事：

1. **带 Bearer token 后 Cloudflare 放不放行** → 决定用 `HttpFomoClient`（轻量）还是 `PlaywrightFomoClient`（兜底）
2. **`swaps` 有没有 `networkId`** → 这是唯一的硬阻断项，没有的话功能 B（共识计数）整体不可用
3. **`swaps` 的分页参数** → 不能翻页就建不了历史基线，功能 A/B 永久降级为不显示

拿到 probe 输出后要做的收尾：
- [ ] 按真实字段名收窄 `poller.normalize_*` 里的 `pick()` 候选列表
- [ ] 核对时间戳单位（**用一笔已知时间的真实交易人工比对**，判错会导致"永远拉不到新数据"的静默失效）
- [ ] 用真实 CA 校准 `models.QUOTE_TOKENS` 计价币白名单
- [ ] 确认「交易次数」字段语义 → 决定是否启用 Q3 的单向否决票（`store.judge_badge` 里那段注释）
- [ ] 真实数据跑通验收用例 1/4/6/8/10

---

## 已知问题

| 问题 | 现状 | 处置 |
|---|---|---|
| `poetry install` 报连不上清华镜像，但 pip 走同一镜像正常 | 已复现（代理环境下） | 用 `requirements.txt` + pip 安装，效果等价；README 里已写明 |
| 全部 API 字段名未经实测 | 设计文档 §11 列了 17 项 | Phase 0 的 `--probe` 逐项核对 |
| `PlaywrightFomoClient` 不支持 swaps 分页 | 刻意为之 | 走该实现时 `stats_ready` 恒 0，功能 A/B 降级为不显示，**主推送不受影响** |

---

## 设计决策速查（改代码前先看这个）

| 决策 | 绝不能改成 | 后果 |
|---|---|---|
| `tick()` 必须「先全部落库，再统一渲染发送」两个独立循环 | 边落库边发 | 同一秒买同一个币的两人会看到 1 和 2 两个不同的共识数，功能 B 当场作废 |
| `judge_badge` 必须在 `upsert_stats` **之前**调用 | 之后 | `buy_count` 已 +1，🌱 永远判不出来 |
| `upsert_stats` 只在 `insert_event` 返回 True 时调用 | 无条件调 | 重复轮询让 `buy_count` 一路虚增 |
| `mark_sent` 只在 `send()` 返回 True 之后调用 | 发之前就标 | 崩在中间的消息永久丢失（下一轮被 `INSERT OR IGNORE` 跳过，再也不会被发现） |
| 共识只在 `user_token_stats` 上聚合 | 在 `fomo_events` 上聚合 | 拆单把一个人算 N 次 |
| 分子分母同一谓词 `active=1 AND stats_ready=1` | 两处不一致 | 算出 `6/8` 这种分子大于分母的输出 |
| 徽章 `None` 一律渲染成 🟢 | 渲染成 🌱 或报错 | 宁可漏标不可错标——🌱 一旦贬值没有第二次机会 |
| `stats_ready=0` 时 `upsert_stats` 直接跳过 | 照常写 | 污染基线，后续必然错标 🌱 |

---

## 单轮提速（2026-08-11）

用户反馈「推送平均要 40s，能优化到 10s 内吗」。先剖析再动手，实测数据如下（69 人名单，含代理）：

| 端点 | p50 | 全员一轮 |
|---|---|---|
| `swaps` | 0.27s | ~5s |
| `balances` | 1.22s | ~35s ← 大头 |
| `trades` | 0.72s（尾部 4.7s） | ~20s |
| `thesis` 25 币（串行） | — | 4.1s |

并发拐点也很明确：69 人 × 3 端点，**12 线程 15.1s、24 线程 10.8s，36 线程反而 66.6s**——
服务端/代理打满后只会换来 504 和重试风暴。所以光加并发不够，必须砍请求量。

### 关键发现：`/feed/tradingActivity` 是「关注流」

探到一个原本没用上的端点。实测 100 条里 **100/100 都属于监控名单、名单外 0 个** ——
这是当前登录账号的关注流，不是全站流。单次 **0.25~0.43s**，100 条覆盖约一小时，
而且买/卖/观点三类混在一条流里。

于是把它当**变更检测器**：一次调用问出"谁刚动过"，再只给这些人拉 swaps。

⚠️ 但它只覆盖**已关注的人**。名单里若有没关注的人，动作永远不出现在流里 ——
所以必须保留 `SWEEP_PER_TICK=12` 的兜底轮转扫描。这不是优化项，是正确性要求。

### 改动清单

| 改动 | 效果 |
|---|---|
| 活动流当变更检测器 + 兜底轮转扫描 | 每轮 swaps 从 69 次降到 ~13 次 |
| `balances` 进程内缓存 + 有交易当场刷 + 8 个/轮轮转 | 每轮 balances 从 69 次降到 ~9 次 |
| `trades` 只拉有新动作的人 | 每轮从 69 次降到 0~2 次 |
| 调度单元从「一个用户」改成「一个请求」 | 慢端点不再占住整条线程 |
| 三个池合并成一个 + 观点扫描挪到后台线程并行 | 串行屏障从 4 道降到 2 道 |
| 推送节流改令牌桶（突发 5 条立发）+ notifier 复用连接池 | 6 条消息的等待从 17.5s 降到 1.5s |
| 重试退避加抖动、`_MAX_ATTEMPTS` 5→3、超时 25s→12s | 一次 504 风暴不再让整轮多花 15s |

### 结果

```
冷启动（建持仓缓存）  22.5s   每轮 API 调用 ~232 次 → ~46 次
稳态                 2.9 / 3.3 / 5.0s
```

轮询间隔随之 45s → 12s。端到端延迟从「45/2 + 40 ≈ 60-85s」降到「12/2 + 4 ≈ 10s」。

### 新增的教训

- **`dict.get()` 返回 None 有两种含义**：key 不存在 vs 值就是 None。
  balances 组装那段若写成 `bal_now.get(uid) or cache[uid]`，`[]`（清仓）会被当假值吞掉，
  那人就永远停在旧持仓上，「N 人仍持有」再也减不下去。必须显式判 `is not None`。
- **补漏池要按「第一池实际拉过谁」扣，不能按「活动流点过谁」扣**。
  冷启动时活动流点名集是空的，而第一池已经全员拉过一遍 —— 按后者扣会整整重打 69 次
  balances（实测 138 次），冷启动直接翻倍。
- **"拉取失败"与"本轮没排到他"必须分开**。混在一起的话稳态下每轮会对五十几个人
  各刷一条 WARNING；而且 `_swap_seen` 若把失败当成"这页就是全部"写进记忆，
  下一轮拉成功时那些条目会被当成"见过的"，这个人的新交易被永久跳过。
- **屏障比请求数更贵**。单个请求最慢能到 3~5s，四道串行屏障各付一次尾延迟，
  就是一轮里最大的一块。合并请求池 + 把观点扫描挪到后台线程，比继续砍请求数有效得多。
- **限流是真的**：该 API 2.5s 内连打 10 次会全部 429。加并发前先确认拐点在哪。

### 对抗性审查确认的三个缺陷（2026-08-11，6 视角 × 每条 3 个反驳者）

提速改完后跑了一轮多视角审查，24 条被反驳掉，3 条确认（去重后）——全部是这次改动引入的：

**1. `_bal_cache` 把「持仓索引延迟」冻结了一整圈（critical）**

有新交易的人，他的 `balances` 与 `swaps` 是**同一个池子里并发发出**的，
拿回来必然是服务端还没索引到这笔交易的旧持仓。这份最不准的快照进了缓存后，
下一轮他既不 hot 也多半轮不到轮转，错误状态被冻结整整一个轮转周期（约 9 轮 ≈ 108s）。

表现：名单集体抢同一个新币的那一两分钟里，「N 人仍持有」系统性偏小——
而那正是这个数字最该准的时刻。改造前每轮全员重拉，下一轮就自愈了。

> `count_holders` 里早就为买入方向写了 `+1` 补偿，说明这个索引延迟是本项目**已确认的事实**，
> 只是缓存把它从"只影响当轮"放大成了"影响一整圈"。

修法：`_bal_dirty` —— 本轮有新动作的人挂脏标记，下一轮无条件重拉一次。
⚠️ 两个信号都要收：`hot`（swaps 差集）**和** `feed_hot`（活动流）。
活动流通常更早知道这笔交易（它就是干这个的），那时 swaps 端点往往还没索引到、`hot` 是空的——
只认 `hot` 正好漏掉最需要补的那一类。（写测试时就是这么发现的。）

**2. 观点采集的后台线程让 playwright 每轮泄漏一套浏览器（critical，两个视角独立发现）**

`tick()` 无条件把 `_fetch_thesis_raw` 提交到每轮新建的线程池，
唯独漏了 `_fetch_snapshots` 里那道 `supports_concurrency` 守卫。
而 `PlaywrightFomoClient` 按 `threading.local` 私有创建整套 chromium、线程退出时不回收
——每 tick 泄漏约 300MB，12s 一轮几分钟吃光内存。

**这与文件注释里明令禁止、且早就修好过的是同一个故障，只是换了个入口进来。**

修法：`_start_thesis()` 统一收口——不可并发的实现留在主线程同步跑；
可并发的实现用**跨 tick 复用**的单线程池（顺带让 curl 会话跨轮复用，省掉每轮的 TLS 握手）；
`batch` 为空时连池子都不建。

**3. notifier 的 429 `retry_after` 没有上限（medium）**

`time.sleep(retry_after + 1)` 直接采信 TG 返回值，而这段 sleep 跑在 poller 线程的 `_dispatch` 里。
一个 `retry_after=300` 就把整个轮询堵 5 分钟，期间 `last_tick_at` 不前进、`/status` 看着像挂了。
`client._retry_after` 早就夹了 60s 上限，这条同类路径当初漏了。修法：夹同样的上限。

### 这轮审查的元教训

- **缓存的代价不是"数据旧一点"，是"把一个短暂的错误状态冻结住"。**
  引入任何缓存时，要问的不是"多久刷新一次"，而是"写进去的那一刻，值是不是恰好最不准"。
  这里就是：唯一会让持仓变化的动作（交易），恰恰保证了此刻拉到的持仓是错的。
- **同一个坑会从新入口回来。** playwright 的浏览器泄漏在 `_fetch_snapshots` 里有守卫、有注释、
  有教训记录，但新开一条后台线程时没人想起它。防线要建在**能力**上（`_start_thesis` 收口），
  不是建在**调用点**上。

### 追加:审查之后自己又发现的三件事

**4. 冷启动把全员挂成脏（写 `_bal_dirty` 的测试时暴露）**

冷启动那轮没有差集基准，`_hot_users` 会把**所有人**判成 hot——但那不是"所有人刚交易过"。
照单全收会让重启后的第二轮白白重拉一遍全员 balances（实测 2.4s → 5.9s）。
修法：`cold = not self._swap_seen`（必须在 `_hot_users` 之前取，那一步会把 `_swap_seen` 填满），
冷启动时只认 `feed_hot`。

**5. 401 落在最后一次尝试上会被降级成 `FomoAPIError`（自己复核审查的"已反驳"条目时发现）**

这条被 3 个反驳者判成"不成立"，但照着代码走一遍是真的：

```
attempt=1: 500 → sleep, continue
attempt=2: 500 → sleep, continue
attempt=3: 401 → auth_retried=False → invalidate + continue
           → while attempt(3) < 3 为假 → 退出循环 → raise FomoAPIError
```

而 `FomoAPIError` 会被 `_fetch_snapshots` 的 `except Exception` 吞成"该项降级为 None"，
tick 照常返回、`last_tick_at` 照常前进、`/status` 一切正常——正是 `_fetch_ok` 自己的注释里
写明"必须避免"的那种失效。**`_MAX_ATTEMPTS` 从 5 降到 3 是我这次改的，等于把这个坑变浅了。**
修法：记 `auth_status`，循环耗尽时若见过 401/403 就以 `AuthError` 收场。

> 教训：对抗性审查的"已反驳"不是免检章。反驳者只投票、不承担后果，
> 而这条的后果（监控静默死亡）严重到值得自己再走一遍代码。

**6. 429 雷群是真的会打起来（跑基准时被 monitor 抓到）**

同一毫秒 15 个请求一起 429（balances×10 + thesis×5 + trades×1），
服务端给的 `retry-after` 又完全相同，于是它们又在同一毫秒一起重试——把刚才那波压力原样重放。
之前两次基准"跑了 400s 没输出"就是撞上了这个。

根因是**峰值并发被低估**：观点扫描在后台线程、快照在主线程池，两者**同时在跑**，
真实峰值 = `fomo_fetch_workers(12) + _THESIS_WORKERS(8) = 20`，而实测拐点在 24 附近。
配置里却只按 fetch 池一处标了上限。

修法两条：`_retry_after` 加抖动（打散雷群的唯一手段）；
`_THESIS_WORKERS` 8→4、`_THESIS_TOKENS_PER_TICK` 25→12（有活动流之后这条路已经降级成兜底，
已关注的人发观点下一轮就直接推，不用等轮转扫到），峰值压回 16。

---

## 自己的交易/观点为什么慢(2026-08-12)

用户实测:「从 APP 下单到 TG 推送 30s 以上」「观点 10:05 发、10:09 才到」。

### 根因:活动流照不到自己

`/feed/tradingActivity` 是「**当前登录账号关注的人**」的流。而人不关注自己 ——
实测自己在 100 条流里出现 **0 次**。于是自己的动作只能靠兜底轮转捞:

| 类型 | 走的路 | 延迟 |
|---|---|---|
| 交易 | `SWEEP_PER_TICK=12`,70 人 → 6 轮 | 平均 36s、最坏 72s ← 用户量到的 30s+ |
| 观点 | 按币轮转,约 400 个币 / 每轮 12 个 | 约 6 分钟 ← 用户等了 4 分钟 |

同一个洞还影响另一类人:**名单里加过、但账号没关注的人**。

### 修法

探到 `/v2/users/current` 能拿到自己的 id(`/v2/users/me` 会被当 userId 校验报 400,
`/me`、`/users/me`、`/auth/me`、`/v2/profile` 全是 404)。于是:

```
_feed_blind = 监控名单 - 我关注的人      # 自然包含自己
```

- 这些人**每轮直拉**,不进兜底轮转 → 交易延迟 = 一个轮询间隔
- 他们刚碰过的币进「观点优先扫描名额」(`_THESIS_PRIORITY_SLOTS=6`,TTL 30 分钟)
  —— 观点几乎总是发在刚买的币上(实测 10:05 买入、10:05 发观点),
  优先名额让观点在下一轮就被扫到,而不是等轮转转过来
- 优先名额**占用**每轮扫描总量而不是叠加,峰值并发不变
- 关注列表缓存 10 分钟;拿不到时全员按"看不见"处理(慢但不漏)

轮询间隔 12s → 10s。端到端 ≈ 10/2 + 3 ≈ **8s**。

### 顺带修掉的两件事(都是压测时暴露的)

**冷启动多打 70 个 trades。** 冷启动的 `hot` 是"全员"(没有差集基准),
不代表全员刚交易过。照它补 trades = 启动瞬间 3N 个请求(70 swaps + 70 balances + 70 trades),
实测足以打爆限流窗口。冷启动改用 `feed_hot` 判定谁真的动过 → trades 70 → 20 次。

**活动流一失败就全员扫,是把限流越滚越大。**
活动流持续失败的头号原因就是**被限流**,这时候把请求量翻五倍只会更严重 ——
实测就是这么滚起来的:feed 429 → 全员扫 70 个 → 压力更大 → feed 又 429。
而活动流挂掉**并不会漏事件**:swaps 端点仍返回最近 50 笔,游标不推进 + `event_id` 去重
保证不丢不重,兜底轮转迟早扫到,只是晚几轮。
所以改成**扩大轮转**(3 倍切片,两轮覆盖全名单约 20s),而不是放弃增量。
效果:同一段压测里 429 从 14 次降到 3 次,稳态单轮从 6-9s 回到 2.3-3.2s。

### 限流的真实形态(实测)

| 端点 | 实测结论 |
|---|---|
| `/v2/users/{id}/swaps` | **27 req/s 打 40 个,0 个 429** —— 没有明显的每秒上限 |
| `/feed/tradingActivity` | 严格得多,2.5s 内连打 10 次全部 429 |

所以限的**不是每秒速率,而是一段时间内的总量,且按端点分别计**。
推论:不需要加限速器,该做的是**减少总请求量**(正是本次改动做的);
但活动流要省着用 —— 生产每 10s 一次(6 次/分)完全安全,
而压测时背靠背连打 4 轮 ≈ 生产 4 倍以上瞬时量,量到的是限流器不是 tick。
(之前两次"跑 400s 没输出"就是这么来的,已在压测脚本里补上真实间隔。)

### 回退:活动流不再当"变更检测器"(2026-08-12,同日)

上面那套「活动流点名 + _feed_blind + 兜底轮转」上线后,用户日志里 `/feed/tradingActivity`
**每一轮都 429**。查下去发现整个方案的前提就不成立。

**实测的限流形态(这是关键):**

| 端点 | 结论 |
|---|---|
| `/v2/users/{id}/swaps` | 27 req/s 打 40 个,**0 个 429** |
| `/feed/tradingActivity` | 每 10s 一次跑两分钟就持续 429(`retry-after: 0`,重试也没用) |

两个端点的限流**完全不是一个量级,且各算各的**。

**于是重新算这笔账:**

- 活动流省下的是「全员 swaps」——而实测 70 人 12 线程只要 **1.2~3.6s**
- 换来的是:那个端点的严格限流、"照不到自己"的覆盖盲区(用户实测 30s+)、
  以及为了绕开这两点堆出来的 `_feed_blind` / 兜底轮转 / 降级分支
- 更糟的是它**一失败就退回全员扫描**,请求量翻几倍 → 限流更严重 → 越滚越大。
  实测就是这么滚起来的:feed 429 → 全员扫 70 个 → feed 又 429

**结论:为了省 1~2s,买了一堆麻烦。回退。**

现在的形态,简单得多:

```
每轮:全员 swaps(没有例外、没有轮转、没有盲区)
      + balances(有交易的当场刷 / 上轮交易过的补刷 / 每轮轮转 8 个 / 其余读缓存)
      + trades(只给本轮有新事件的人)
      + 观点:按币轮转 12 个(后台线程,与采集并行)
              其中优先名额留给「刚动过的币」—— 观点几乎总是发在刚买的币上
活动流:降到每 6 轮一次,只负责捞"发在老仓位上"的观点。挂了完全不影响买卖推送
```

实测:冷启动 10.7s,稳态 **2.8 / 2.9 / 2.8s**(非常稳),429 从 14 次降到 4 次
且全部由带抖动的重试自愈。轮询间隔 10s → 端到端 ≈ 5 + 2.8 ≈ **8s**,每个人都一样,
不再有"自己反而最慢"这种事。

### 教训

- **省下来的是什么,换来的是什么,要放在一起算。** 活动流那条路单看"1 次调用替掉 70 次"
  非常漂亮,但省下的只有 1~2s,而代价是一个独立限流的端点 + 一个结构性盲区 +
  一套降级分支。删掉它之后,代码更短、更快、还没有盲区。
- **限流要按端点分别实测,不能只测一个就推广。** 先测 `/v2/users/*` 得出"27 req/s 没事",
  差点据此认定"不需要关心限流" —— 而真正卡住的那个端点严格几十倍。
- **降级策略不能反向加压。** "活动流失败 → 全员扫描"看起来是保守稳妥的兜底,
  实际上失败的头号原因就是限流,这个兜底恰好把火浇成了油。
  正确的做法是判断"失败会不会真的漏数据"—— 这里不会(游标不推进 + 主键去重),
  所以应该**降级得更轻**,而不是更重。

---

## 两个展示功能(2026-08-12)

### 特别关注 `/star`

`watch_users.starred` + `/star` `/unstar`,`/list` 里 ⭐ 的排最前、标题显示星标人数。

推送形态:`🌱 ⭐ 名字 (@handle) · 首次建仓 · 【$Plumber】`

⚠️ **星标不能顶到行首**。行首那个字符是聊天列表预览里唯一的扫描锚点,
⭐ 顶掉它之后所有特别关注的消息在列表里长得一模一样,买入卖出当场分不出来 ——
而那恰恰是最需要一眼看清方向的一批人。所以它跟在事件 emoji 后面。

⚠️ 星标是**纯展示**:不改徽章、不改共识分子分母、不改采集频率。
查询失败降级为"不打星标",绝不挡住推送。

**关于"彩色币名":Telegram 的 Bot API 不支持任意文字颜色。**
允许的标签只有 `b / i / u / s / code / pre / a / blockquote / tg-spoiler`,
没有 `font`、没有 `style`。真能出颜色的只有两条:
  1. diff 代码块(`<pre><code class="language-diff">`,加号行绿、减号行红)——
     但它是等宽的块级元素,会把标题行拆开,还吃掉行内链接
  2. 彩色 emoji(🟩🟥 这类)
所以默认用「加粗 + 方括号」做强调,颜色交给行首的 🌱/🟢/🔴 承担。
改样式只需要动 `formatter._style_symbol()` 一个函数。

### 币龄 `🕐 币龄 3H`

数据源:`balances[].tokenFilterResult.token.createdAt`(unix 秒)。**已经在每轮都拉的数据里,零额外请求。**

⚠️ 必须取 `token.createdAt`(**合约创建**),不是 `tokenFilterResult.createdAt`
(那是**交易对/池子**的创建时间)。两者对新币几乎一样,对老币差得离谱 ——
实测 USDC:`token.createdAt` = 2020-10-13(对),`tfr.createdAt` = 2024-03-20(某个池子建的时间)。
拿错字段的话一个老币会显示成"币龄 5D",而"新币"正是这个项目最关心的信号。

实测覆盖率 1058/1061 = 99.7%;抽查 USDt 8.7Y、WETH 8.7Y、USDC 8.0Y 全部与链上实际一致。

格式:`8M / 3H / 5D / 2MO / 1.4Y`。
⚠️ 分钟用 `M`、月份用 `MO` —— 单独一个 `M` 在币圈语境里会被读成市值。
⚠️ 落库(`fomo_events.token_created_at`):补发的消息也要能显示,
而 formatter 是纯函数不查库(§10.4 铁律 7)。币龄是恒定值,不像市值那样会过期。

### 教训

- **NaN 能绕过 `if x < 0`**。`fmt_token_age` 最初只挡负数,NaN 一路落到最后一支
  渲染成 `"nanY"` —— 单测当场抓到。范围判断遇上浮点必须显式 `math.isfinite`。
- **`to_row` 的字段集合有专门的单测钉着**。加一列 `token_created_at` 立刻红,
  提醒同步改 `insert_event` 的具名绑定 —— 这正是那条测试存在的理由。

### /hot 改成按倍数排序 + 首买人 + 基准市值(2026-08-12)

原来按买入人数排。问题是这个榜要回答的是「名单挖到了什么金狗」——
一个 60 倍但只有一个人买的币会沉到看不见的地方,而那恰恰是最该看到的。

新形态:

```
1. $Plumber · 🚀 66.9x · 👥 11 人买入(69 笔)
   💎 $41.90K → $2.80M · 💰 名单买入 $138.31K
   🥇 @brrrgrrrz 13 小时前首买
   👤 @remusofmars @collectible @Pasterniq +7
   🧬 Solana
   GCa9…
```

- 倍数放进标题行:它现在是名次的依据,排序键必须一眼可见,否则名次看着像随机的
- `💎 基准 → 现在` 把倍数的两个输入都摊开,用户能自己验算
- `🥇` 单独一行给首买人

SQL 改用 CTE + `ROW_NUMBER()`,让"首买人"与聚合共用同一套谓词
(active=1 AND stats_ready=1 + COUNTABLE_REASONS)——
不共用的话,首买人可能是一个根本没被算进 `👥 N 人` 的人。

⚠️ **`first_buyer` 与 `first_mcap` 可能不是同一行**,展示时必须分行写:
`first_buyer` 取真·最早那笔,`first_mcap` 取最早**且有市值**那笔。
市值只来自 balances,而 balances 快照晚于 swaps 索引 ——
"名单第一个人抢到新币"的那一刻他还没出现在自己的持仓里,那行的 market_cap 就是 NULL。
合成一句「@某人在 $42K 时买入」就是在断言一件我们并不知道的事。

⚠️ 算不出倍数的必须**显式**排最后:`ORDER BY (mult IS NULL), mult DESC, …`。
SQLite 里 NULL 在 DESC 中确实排最后,但依赖这个隐式行为等于把意图藏起来 ——
写出来,下一个人改排序时才不会踩掉。

副作用(已接受):单人小额买入的高倍币会冒到前排(如上面 #2,1 人 $474 买了个 11x)。
这正是用户想看的信号;`👥 N 人` 就在标题行里,判断权交给用户。

### /hot 再展开前 3 个买家(2026-08-12)

`👤 @a @b @c +7` 这一行只有名字,看不出谁下的注大、谁只是跟了一手。改成:

```
   🥇 @brrrgrrrz · $783.25(3 笔) · 13 小时前
   🥈 @remusofmars · $995.00 · 13 小时前
   🥉 @collectible · $4.99K · 4 小时前
   👤 另有 8 人 · 🧬 Solana
```

- 名次是**买入先后**,不是金额大小。榜里经常出现 🥈 比 🥇 买得多的情况
  (先摸到的人未必下注最重),所以页脚显式写了「🥇🥈🥉 = 买入先后」——
  奖牌通常被读成"金额最大",不点明就是误导。
- 金额是**窗口内累计**不是首笔:一个人分五笔建仓,只报首笔会把他的实际投入
  低报成五分之一,而"谁下的注最大"正是这一行的价值。
- 「另有 N 人」与链名合并一行,省掉一行高度。

⚠️ `HOT_TOP_BUYERS` 与 `MAX_HOT_ROWS` 是绑在一起调的:每多展开一个买家就是
每个币多一行,乘以榜行数直接顶 TG 的 4096 字符硬上限。
所以榜行数同步从 12 降到 10 —— 实测当前输出 2551 字符,留了足够余量。

---

## 启动刷 504 + Ctrl+C 退不掉(2026-08-12)

用户报了两个现象,是两个独立问题。

### 1. 启动时满屏 `FOMO 服务端错误 504 | …/balances`

两个原因叠在一起:

**(a) 冷启动把全名单的 balances 一次性打出去。**
`/balances` 是最重的端点(单个 100~400KB),70 个一起打直接把源站打到
**504 Gateway Timeout** —— 注意这是 origin 超时,**不是限流**,重试也是 504,
结果缓存反而填不满。
修法:`_BALANCE_WARMUP_PER_TICK=10`,分批预热。
⚠️ 预热要**轮转取**而不是每轮取头几个 —— 上游抖动时队头那几个会一直失败,
固定取头部等于让后面的人永远排不上,缓存永远填不满。

**(b) 每次重试都打一条 WARNING。**
上游一抖就是几十行刷屏,而这些失败**已经被处理掉了**
(重试 + balances 有缓存兜底)。真正要紧的 ERROR 会被淹掉。
修法:逐条降到 DEBUG,每轮汇总成一行:

```
上游抖动 | 服务端 504×9 · 最终降级 balances×1 · 还有 70 人的持仓未预热(已重试/降级,不影响推送)
```

实测同一段压测:WARNING 从 34 行降到 4 行,每行都有信息量。

> 顺带确认:这次的 504 是 **FOMO 源站真的在劣化** —— 同一个 `/balances`
> 早上 p50 1.2s,现在单次 10.4s。不是我们打太狠。所以单轮涨到 11-13s,
> 轮询间隔相应从 10s 调到 15s(贴着单轮耗时设会一直刷 skipped)。

### 2. Ctrl+C 要按很多次、等很久才退出

调度器停了,但线程池里几十个请求还在跑:每个可能卡在重试退避的 `time.sleep()` 里,
或者卡在 12s 的 HTTP 超时上。`ThreadPoolExecutor` 的 atexit 钩子会 join 它们
（用户日志末尾那段 `_python_exit → t.join() → KeyboardInterrupt` 就是它）。

修法三处:
- `client._STOP`(全局 Event)+ `sleep_or_stop()`:所有退避改成**可打断的等待**;
  已在停机则排队中的请求直接放弃、不再发出
- `poller._throttle_send` 同样改成可打断 —— 积压几十条时它一次要等好几十秒
- `Poller.close()` 关掉常驻的观点线程池;cli 的 `finally` 里**先喊停、再关调度器**

实测:从"发出停机信号"到"tick 收尾" **0.2s**(改之前要十几到几十秒)。

### 教训

- **"已经处理掉的错误"不该用 WARNING 逐条播报。** 判据是:这条日志能让用户**做点什么**吗?
  不能的话就降级 + 汇总。刷屏的真实代价是把能做点什么的那几行淹掉。
- **限量取批次时要轮转。** `[:N]` 看着人畜无害,但只要队头会失败,
  后面的人就永远排不上 —— 而"队头会失败"恰恰是限量的起因。
- **可中断的等待要覆盖所有以秒计的 sleep**,不只是重试退避。
  漏掉推送节流那一处,Ctrl+C 照样走不掉。

### /hot 前三名加"进场市值"(2026-08-12)

```
🥇 @brrrgrrrz · 💎$41.90K · $783.25(3 笔) · 15h
🥈 @remusofmars · 💎$98.25K · $995.00 · 14h
🥉 @collectible · 💎$4.19M · $4.99K · 6h
```

一眼能看出"谁在什么位置进的":🥇 在 $41.9K 摸到,🥉 进场时已经 $4.19M(涨了 100 倍)。
这是"跟谁"最直接的依据 —— 名次只说明先后,进场市值才说明这一手值不值得跟。

⚠️ mcap 取的是他**最早一笔有市值**的那笔,不是最早那笔。
市值只来自 balances、而 balances 快照晚于 swaps 索引 ——
抢到新币那一刻本人还没出现在自己的持仓里,那行 market_cap 就是 NULL。
不往后找的话,恰恰是"抢得最早的人"没有进场市值可显示,而那正是最该看到的一个。

⚠️ 多笔建仓时这个数只代表**第一笔**的位置,所以后面的笔数必须留着 ——
只看一个市值会把"分五笔从 40K 加到 200K"读成"他在 40K 一把梭"。

时间改成紧凑写法(`15h` 而不是「15 小时前」):一行要塞名字+市值+金额+时间,
四个汉字就是手机上换行的那根稻草。与币龄行用同一套单位。

### /hot 补峰值市值 + 「现在」两个字(2026-08-12)

用户看到 `🥉 @collectible · 💎$4.19M`,而那一行上面写着 `💎 $41.90K → $2.68M`,
于是问"第三名 4M 才进不应该吧"、"$2.68M 是 ATH 吧"。

查库之后两个问题都有明确答案:

**排序是对的。** $Plumber 的真实买入序列:

| # | 谁 | 时间 | 买入时市值 |
|---|---|---|---|
| 1 | brrrgrrrz | 14:20 | $41.9K |
| 2 | remusofmars | 14:31 | $98.3K |
| 3 | collectible | **22:54** | $4.19M |

@collectible 确实是第 3 个买的,但比第 2 名晚了 **8 个半小时** ——
只有两个人吃到早期,之后断档 8 小时,剩下 9 个人全在 $4.19M 附近一拥而上。

**$2.68M 是当前市值不是 ATH**(`token_snapshot.market_cap` 每轮从 balances 刷新)。
但用户的直觉指向了一个真问题:箭头 `$41.90K → $2.68M` 读起来像"起点 → 最高点",
于是 $4.19M 的进场看着就不可能。**真相是这个币冲到 $4.19M 之后回落了**,
追高那批人正套着 —— 而这恰恰是最该看见的信息,却被现在的显示藏起来了。

修法:
- 文案写明「现在」:`💎 $41.90K → 现在 $2.91M`
- 新增 `token_snapshot.max_market_cap`(每轮取 max,只增不减),
  明显回落过才显示:`· 峰值 $4.19M`
- ⚠️ 迁移**用历史买入记录回填**(`MAX(fomo_events.market_cap)`),不从今天开始重新攒:
  已经冲高回落的币恰恰是最该看到峰值的那些,而它多半不会再冲了 ——
  不回填的话它永远没有峰值可显示。`fomo_events.market_cap` 天然采样了上涨过程。
- ⚠️ 那一轮没有市值时不能把峰值抹掉:清仓后的币仍会被 trades 带进来,
  但那时 market_cap 是 NULL。

效果:

```
1. $Plumber · 🚀 69.5x · 👥 11 人买入(69 笔)
   💎 $41.90K → 现在 $2.91M · 峰值 $4.19M · 💰 名单买入 $138.31K
   🥇 @brrrgrrrz · 💎$41.90K · $783.25(3 笔) · 15h
   🥈 @remusofmars · 💎$98.25K · $995.00 · 15h
   🥉 @collectible · 💎$4.19M · $4.99K · 6h
```

一眼看出 @collectible 买在峰值上、现在套着 30%。

⚠️ 叫「峰值」不叫「ATH」:它只在名单里有人持有、且轮询到的时刻才采样,
币在我们看它之前冲过多高无从知道。名字必须诚实。

### 教训

- **用户说"这个数不对"时,先查库再解释。** 这次两个疑问一个是"其实是对的"、
  一个是"数是对的但展示有误导",处置完全不同 —— 靠猜会两个都答错。
- **箭头 `A → B` 天然被读成"起点→终点/最高"。** 中间回落过就必须显式写出来,
  否则下面任何一个高于 B 的数字都会被当成 bug。

### 倍数改成"最早买入 → 峰值"(2026-08-12)

原来倍数 = 现价 ÷ 最早买入市值。问题:这个榜要回答"名单挖到了什么金狗",
而金狗的价值在于它**跑出来过**多少 —— 一个冲到 100x 又回落到 60x 的币,
排在一个稳在 70x 的币后面是不合理的,前者才是那次真正抓住了的机会。

现在:`🚀 最高 99.9x`(峰值 ÷ 名单最早买入时市值),排序同样按它。

⚠️ 标题必须写「最高」两个字。不写的话,一个回撤过的币会被当成"现在还有这么多",
而下面 💎 行明明写着现价更低 —— 两个数打架,用户只会当成 bug。
⚠️ 现价照常在 💎 行里(`$41.90K → 现在 $3.16M · 峰值 $4.19M`),回撤自己看得见。

---

## 跟单功能:第一步(信号 + 纸上跟单)(2026-08-12)

用户要「一个币有 3 个以上关注的人买入就自动买」,参数可配。

### 先用真实数据把这条规则跑了一遍

在用户自己 50 个币的记录上回测「第 N 个人买入时跟、持有到现在」:

| 阈值 | 样本 | 盈/亏 | 中位数 | 平均 |
|---|---|---|---|---|
| ≥2 人 | 50 | 24/26 | 0.99x | 1.64x |
| ≥3 人 | 27 | 12/15 | 0.98x | 1.10x |
| ≥5 人 | 7 | 4/3 | 1.00x | 1.49x |

**最关键的一条:`$Plumber` 在 N=2 是 31.62x,在 N=5 是 0.74x。**
同一个币,`/hot` 上写着「最高 99.9x」。
原因是名单的买入**不是均匀分布**的:前 2 个人在 $41.9K/$98K 进,
之后断档 8 个半小时,剩下 9 个人全挤在 $4.19M。
「N 个人买入」成立的那一刻,往往正是拉升已经发生完的那一刻 —— 阈值越高越晚,
而这个信号的全部价值就在于早。

⚠️ 回测没算手续费/滑点/gas,样本仅约一天,而且是**回看**。
   这就是默认先走纸上跟单的原因 —— 需要**前瞻**数据才能调参数。

用户据此把阈值从 ≥3 改成了 **≥2**。

### 边界(不可动摇)

**真实成交永远由用户点击触发。本项目不做无人值守自动下单。**
`CopyConfig.paper_only` 默认 True,要接真实下单必须显式 `/copy real`。

技术上也只有一条路:FOMO 的 swap 是**链上交易**(记录里有 recipient / platformFeeAddress),
由 Privy 嵌入式钱包在浏览器里签名。我们手上的 `fomo_session.json` 只是 API 的读 token,
**签不了交易** —— 真实下单必须走 Playwright 驱动真实网页(项目已有持久化登录 profile)。

### 已完成

- `src/copytrade.py`:纯函数判定层(`decide` / `pnl`),不碰 IO,可完整单测
- `copytrade_signals` 表:**主键就是 (network_id, token_address)** ——
  「单币只跟一次」由主键保证,不是先 SELECT 再 INSERT
  (后者在两个 tick 撞上时会重复建仓,真实下单模式下就是真的多花一份钱)
- `/copy` 看/改参数(on/off · paper/real · buyers · window · age · mcap · amount · daily · starred)
- `/paper` 台账 + 按市值比折算的盈亏
- poller 每轮判定,整段包 try —— 跟单是附加功能,任何问题都不该影响主推送

### 判定层的几个刻意选择

- **拿不到币龄 / 拿不到市值一律不跟**,而不是放行。这两个值恰恰是**新币最容易缺**的
  (都来自 balances,而 balances 快照晚于 swaps 索引)—— 放行等于筛选器专挑最该拦的放过去。
- **每日上限放在最后判**。放前面的话,达到上限之后所有币的原因都变成「已达当日上限」,
  就看不出哪些币其实本来也不合格 —— 而那才是调参数时要看的。
- **盈亏用市值比折算**,不记 token 数量。我们没有真实成交回执,记数量等于把滑点、
  手续费、成交价假装成理想值。市值比至少口径诚实:它就是"这个币涨跌了多少"。

### 还没做

一键确认下单:TG inline 按钮 + `callback_query` 处理(bot 目前只收 `message`),
以及 Playwright 驱动 FOMO 买入页。后者我需要先看真实 DOM,
而且**第一次真实成交必须由用户自己用最小金额验证** —— 我不会为了验证而下一单。

### 跟单第二步:TG 确认按钮(2026-08-12)

按钮这一半做完了,**执行器仍未接入**(见下)。

- `notifier.send(buttons=[(文字, callback_data)])` 发内联键盘
- `answer_callback` / `edit_message`:点完改写原消息并**清掉键盘**
- `bot._handle_callback`:权限门与命令层完全一致

⚠️ **`allowed_updates` 是白名单。** 不写 `callback_query` 的话按钮点击永远收不到,
   而且没有任何报错 —— 表现是"按钮点了没反应",极难联想到是这里。
⚠️ **`callback_data` 上限 64 字节**,而 Solana 的 CA 就有 44 字符。
   用 `网络:CA前12位` 当键(实测 23/24 字节)。超长时 TG 整条 400,
   而消息会**没有按钮地发出去** —— 你以为能点,其实不能。所以超长的按钮宁可丢掉,
   消息照发(里面有 CA,手动买得了)。
⚠️ **先抢占状态再执行**。连点两次、或消息被转发后两个人各点一次,
   都必须只成交一次;先执行后改状态在这两种情况下会**买两次**。
⚠️ 权限门不能比命令层松:消息可能被转发到别的群,那里的人点按钮同样产生 callback,
   而这条路径**会花钱**。
⚠️ 执行器没接入时**明确写 failed**,绝不写 filled。
   台账显示"已成交"、`/paper` 给你算着盈亏、而你根本没有这个仓位 ——
   那是这个功能最坏的一种失效。

### 仍未做:Playwright 下单执行器

需要先看 fomo.family 买入页的真实 DOM。而且**第一次真实成交必须由用户自己
用最小金额验证** —— 我不会为了验证而下一单。
在那之前点「✅ 买入」会明确回一句"没有成交",并把状态记成 failed。

### 跟单第三步:Playwright 执行器(2026-08-12)

看了 fomo.family 代币页的真实 DOM(只读:goto + 读 DOM + 截图,不点任何按钮)。
买入面板比预想简单:

```
[Buy] [Sell]                  ← tab
<input placeholder="0">       ← 金额
[$10] [$100] [$500] [$1000]
$47.90 available   [Max]
[ Buy <Symbol> ]              ← 成交按钮
```

**演练已跑通**:`$10.00` 填入、报价 `3.8K Plumber / ~$9.93`、手续费 `$0.10`、
`Buy Plumber` 变蓝可点 —— 停在这里,没点。

#### 演练当场抓到的一个 bug

第一次跑直接报「找不到成交按钮」。查下来是**时序**:
这个按钮在报价算出来之前是 `disabled=True`,填完金额约 1~2 秒才变 `False`。
我原来只等 800ms 就去查,查到的是灰的。
修法:`wait_for_function` **等它变可点**,而不是立刻断言。
真的一直灰着才报错,并说清是"余额不足 / 金额超限 / 报价没拉到"。

> 这正是 dry_run 存在的意义 —— 这个 bug 如果留到真实成交那次才发现,
> 代价是一次失败的下单,而不是一行日志。

#### 四道校验(每一道都是"买错"的防线)

1. **登录态** —— 未登录时页面照样渲染,但面板换成登录引导,盲点会点到登录按钮
2. **URL 里必须含这个 CA** —— 搜索跳转/重定向可能把你带到别的币,**买错币不可逆**
3. **读回输入框** —— 受控组件/千分位/失焦重写都可能让 fill 的值与页面实际值不一致,
   "以为填了 50、实际 5000"必须在点下去之前发现
4. **等按钮可点** —— 见上

#### 三层刹车

- `enabled=False`(默认):根本不判定信号
- `paper_only=True`(默认):只记账,不碰浏览器
- `dry_run_execute=True`(默认):走完全流程但**不点成交**

要真实成交必须显式:`/copy on` → `/copy real` → `/copy live`。

⚠️ 演练模式**绝不记 filled**(记 rejected)。记 filled 会让 `/paper`
给一个并不存在的仓位算盈亏。
⚠️ 真实成交后回执写「已提交,请到 APP 核对」而**不是**「已成交」:
链上确认要时间、页面 toast 文案也未实测,在没有回执的情况下报成功是最坏的失效。
⚠️ 点击成交前**先把状态抢占成 executing**,否则第一次还在跑浏览器时
第二次点进来会看到 pending,于是买第二单(有单测钉住)。

#### 仍需用户自己做的一件事

**第一次真实成交必须你自己用最小金额验证。** 我不会为了验证而下一单。
另外实测你的可用余额是 $47.90,而默认单笔 $50 —— 先 `/copy amount 10`。

---

# ============ 无人值守自动跟单(2026-08-12 起)============

# 无人值守自动跟单 —— 改造清单

来源:2026-08-12 对抗性审计(30 条发现 → 反驳 20 条 → 存活 10 条),
加我自己复核的 4 条事实。结论:**executor.py 本身几乎不用动**,
要补的全是它周围那圈原本由「人的手指」承担的护栏。

⚠️ **A 档 7 项全部做完之前,不要打开自动执行。**
⚠️ 做完之后开关仍然**默认关闭** —— 什么时候开是用户的决定,不是我的。

---

## A 档 · 不做就会花错钱(硬门槛)

- [x] **A1. 独立开关 `auto_execute`,不复用 `paper_only` / `dry_run_execute`** ✅ 2026-08-12
      `CopyConfig.auto_execute=False` + `auto_blockers()` + `/copy auto|manual`。
      开自动时当场列出还差什么,**被拒绝时不落库**。
      改 `copytrade.py` CopyConfig / `bot.py` _COPY_FIELDS / `/copy` 面板文案。
      判定必须是 `enabled and not paper_only and auto_execute` 三者同真。
      **为什么**:我已经让用户之后要跑 `/copy real` + `/copy live`(验证顺序)。
      如果自动挂在这两个标志上,他照我说的做的那一刻就变成无人值守了。
      (审计说「库里很可能已经是 False」——**实际不是**,现在还是 paper_only=true。
       但结论不变,理由换成上面这条。)

- [x] **A2. `_check_copytrade` 加 catchup 守卫** ✅ 2026-08-12(带对照组测试)
      `poller.py:1520` 开头 `if self._catchup_since: return`。
      **为什么**:自己 grep 验证过,`_catchup_since` 只在 543/586/1343/1447 出现,
      **全在推送侧**。停机 ≥45min 重启后,推送降级成汇总,跟单却拿到整夜积压,
      而 `entry_mcap` 取的是**现在**的市值 → 按今天的价买昨晚的信号。
      ⚠️ 现在有一层**意外**阻尼(冷启动轮 balances 只拉头 10 人 → 多数币
      `created_at=None` → SKIP_NO_AGE)。那是巧合不是防线:`max_age_hours`
      一设成「不限」就没了。

- [ ] **A3. 真实成交路径的 `entry_mcap` 不能用 `token_snapshot` 旧值**
      `poller.py:1549` 现在是 snapshot 优先;snapshot 对已清仓的币不再更新。
      改成本轮 `_token_meta` 优先,拿不到就跳过(不回落)。
      **为什么**:自动模式下它同时是**筛选闸门**和**台账成本**,不再只是卡片上一个数字。

- [ ] **A4. `set_copy_status` 改真正的 CAS;自动路径不用 `pending`**
      `store.py` UPDATE 加 `AND status = ?` + 用 rowcount 判抢占;
      自动路径落 `auto_executing`,且不挂 TG 按钮(避免人机双入口)。
      **为什么**:改造后 poller 主线程与 bot 线程同时写这张表,而 `get_conn()`
      每次开独立连接,只有 WAL,没有跨线程互斥。真实后果不是买两次
      (profile 锁天然互斥),**是状态互相覆盖**:钱花了、台账写「未成交」、
      TG 弹 ❌ 反过来诱导手动重试。

- [~] **A5. per-token try;失败必须写 `failed` + 发 TG**
      ✅ per-token try 已做(抽出 `_copy_one`,单币异常不影响本轮其余币)。
      ⬜ 「写 failed + 发 TG」等 B1 接上真实执行后一起做 —— 现在还没有会花钱的失败。
      `poller.py:1538` 循环体内每币独立 try;`poller.py:632` 换 `logger.exception` 带 symbol/CA。
      **为什么**:三重叠加 ——
      ① 循环体无内层 try,executor 有 10 处 raise,一次异常掀掉整轮剩余的币;
      ② `record_copy_signal` 先 commit 再买入 → 异常后该行以 pending 永久留库,
         下轮主键冲突 continue,**这个币再也不会被跟**;
      ③ 全项目唯一写 `failed` 的地方在即将删掉的回调里,日志无 TG sink →
         「一次真实花钱失败」被伪装成「跟单信号判定失败(不影响推送)」。

- [x] **A6. 按天的**金额**闸门 + 修 `daily_max=0` + 配置校验** ✅ 2026-08-12
      `daily_spend_usd` + `/copy spend` + `copy_spent_today`(只数会出账的状态)
      + 逐字段类型收敛 + 面板不再把「不限」渲染成「3/0 单」。
      判定是「这一单下完会不会超」,不是「现在超没超」。
      ⚠️ 顺手修了一个原本就存在的洞:两个上限的分子是循环**外**查的,
      一轮命中多个币时不自增 = 一个 tick 就能捅穿当日上限。
      加 `daily_spend_usd`;`copy_taken_today` 统一口径(现在 failed/演练/rejected 也吃额度);
      `load_copy_config` 加类型收敛(JSON 里 null 会让 `None > 0` 抛 TypeError → 静默停摆)。
      **为什么**:`copytrade.py:134` 是 `if cfg.daily_max > 0 and ...`,
      `/copy daily 0` 是「不限」不是「停」,而面板显示「今日 3/0 单」读起来像已限住。

- [ ] **A7. 下单前自检浏览器凭据 + 失效告警**
      抽 `check_login()`;纳入 `--check`;启动时 + 切 auto 时各跑一次。
      **为什么**:PROFILE_DIR 是**第二套凭据**,全项目没有任何代码刷新它。
      ⚠️ 另查到:`--login --cdp` 分支在 `auth.py:444` 就 return,而
      `PROFILE_DIR.mkdir` 在 `:452` —— **走 cdp 这个目录一次都不会写**,
      而 README 恰恰把 `--cdp` 推荐成「最可靠」。
      (现状:profile 124MB、cookie 今天 15:09 写过,是好的。这是**将来**的坑。)

## B 档 · 不做会漏单 / 拖垮监控

- [x] **B1. 执行搬出 tick 线程 → 单线程队列** ✅ 2026-08-12 · 新模块 `src/copyworker.py`
      单线程不是保守,是 profile 锁的物理约束:同一个 PROFILE_DIR 同时只能被
      一个浏览器进程占用,并发提交只会换来一堆「浏览器起不来」。
      队列深度 3、入队到执行超过 120s 就不买(晚三分钟买进去是**另一笔交易**)。
      满了当场拒绝并记 failed + 发 TG —— 留在 auto_queued 的话这个币因主键冲突
      再也不会被跟,而没人知道它丢了。
      状态流转:`auto_queued` →(CAS 抢占)`auto_executing` → `filled`/`failed`。
      **不走 pending** —— 否则「排队中」和「可点确认」两个入口会互相覆盖终态。
- [x] **B2. 「立刻停手」** ✅ 2026-08-12
      每一单执行**前**重查配置,执行**途中**再由 `should_stop` 回调查三次。
      ⚠️ 急停只在 `submit.click()` **之前**有效。过了那一下,「停」只会让程序
      不去读回执 —— 钱花了、状态没写、你还以为停住了。这是最坏的结果。
- [x] **B3. 遍历顺序按信号质量排** ✅ 买家数降序 → 最早那笔买入时间升序
      原来是 `sorted(keys)`,排序键是 CA 字典序。有人盯着时只影响按钮顺序;
      自动之后额度有限,这个顺序**决定当天的钱买了哪几个币**。
      ⚠️ 改完当场被自己的测试抓到一次回归:买家数查询挪到循环外之后,
      跳出了 A5 那道 per-token 兜底 —— 一个币查询失败又能掀掉整轮了。已逐个兜底。
- [x] **B4. 自动成交的 TG 回执** ✅ 四档分开:已成交 / 已点击待核对 / 未成交 / 超时未提交
- [x] **B5. 启动对账** ✅ 2026-08-12 · `reconcile_inflight` + `expire_stale_pending`
      `auto_executing`/`executing` → `unknown` + **主动推 TG**(只写进库等人翻 = 没认领)
      `auto_queued` → `failed`(worker 先 CAS 再执行,停在这儿就一定没跑)
      `pending` 超 24h → `expired`(TG 按钮不会过期,三天前那条点下去买的是今天的价)
      ⚠️ `auto_executing` **绝不能**自动判 failed:CAS 抢占在点击之前,
         但点击到写终态之间还有一段 —— 钱可能已经出去了。

## C 档 · 锦上添花

- [x] C1 `/copy` 面板一眼看出「现在是无人值守」✅ 模式行 + `/paper` 每个状态都有符号
      (落到默认的「•」意味着「结果未知、钱可能已出去」这种最该被看见的行,
       看起来和别的一模一样)
- [x] C2 补测试 ✅ catchup / CAS 抢占 / 单币异常 / 金额闸门 / 队列全套 / **急停只打在点击之前**
- [x] C3 `/paper` 标注行情新鲜度 ✅ 超 1 小时标「⚠️行情停在 3d 前」,够新时不占屏
- [x] C4 每日对账推送 ✅ 跨 UTC 日的第一轮补报前一天;「待核对」单独一行放最后

---

## 复盘

**A 档 7/7 · B 档 5/5 · C 档 4/4 全部完成。377 passed。`auto_execute` 仍然默认关闭。**

### 这次改造真正学到的

1. **审计说的不一定对,但审计问的一定值得查。**
   30 条发现反驳掉 20 条。存活的 10 条里还有 2 条的**理由**是错的
   (A1 说"库里很可能已经是 paper_only=False" —— 实际不是),但结论仍然成立,
   只是理由要换。所以「已反驳」不是免检章,「已确认」也不是。

2. **最好的数据源常常不在你先想到的那张表里。**
   A3 本来只打算把 snapshot 优先改成 balances 优先。是**测试挂了**才逼出
   真正的答案:买入事件自带 `market_cap`,那就是「名单里那个人刚刚是在什么价位买的」,
   比任何快照都准。84% 覆盖率,一行代码。

3. **加护栏的动作本身会拆掉别的护栏。**
   B3 把买家数查询挪到循环外做排序,顺手就把 A5 那道 per-token 兜底从背后绕过去了。
   当场被测试抓到。**改动的半径永远比你以为的大一圈。**

4. **「停」这个动作有一条不可逾越的线。**
   急停检查点绝不能出现在 `submit.click()` 之后。过了那一下,
   「停」只会让程序不去读回执 —— 钱花了、状态没写、你还以为停住了。
   同理:关闭时不能替在途单写 failed,对账时不能把 unknown 判成 failed,
   金额上限要把 unknown 按「花了」算。
   **不确定的时候,「可能花了钱」永远比「没花钱」安全。**

5. **措辞是功能的一部分。**
   「已成交」/「已点击待核对」/「未成交」必须是三档而不是两档:
   把第二档报成成功会让人以为没事,报成失败会诱使人手动再买一次。
   同理「今日 3/0 单」会被读成「已经限住了」,实际是闸门开着。

### 还没做的(有意留着)

- executor 内部**点击之后**没有中断点 —— 这是设计,不是遗漏
- 没有卖出 / 止盈止损。这个项目到目前为止只会买
- 滑点保护依赖 FOMO 页面自己的设置,程序没有独立控制
- `--login --cdp` 仍然不写 PROFILE_DIR(现在至少会警告)。
  真要修得让 executor 也支持 cdp attach,而那与无人值守冲突

### 开自动之前建议先走的路

`/copy real` + `/copy live` 之后是**人工确认**模式:信号照常推、带确认按钮,
点一次买一次。先在这个模式下跑几天,再考虑最后那步 `/copy auto`。

⚠️ 护栏拦得住程序出错,拦不住信号本身不赚钱 ——
≥2 人门槛在用户自己 50 个币上是**盈 24 / 亏 26、中位数 0.99x**,且未计手续费与滑点。


---

## /ca 查 CA 命令（2026-08-25 未完成，明天继续）

分支 `feat/ca-lookup`，工作区干净，两个提交：
- `d67ad49` 新增 /ca：查观点（全站）+ 名单本地记录
- `1070d0e` 修复 9 项审查缺陷

测试 517 passed，ruff 干净。**未合并到主干、未 push。**

### 已确认能做到的（探过接口，别再重新探）
- `get_token_thesis(CA, 数字链ID)` 对**任意 CA** 都能查，包括库里一行都没有的币
- `networkId` 必须传数字（`56`），传内部别名 `bsc` 直接 400
- `comment` 是嵌套 dict（`comment.comment`），不是纯文本
- `threshold=0` 已写死在 client 里，必须传，否则小仓位观点被静默丢弃
- **喂错链返回空，不会返回别的币的数据** → 「依次试各链」安全
- **没有代币元数据端点**，探过 6 个路径全 404 → 市值/价格/创建时间只有本地库里有的币才拿得到
- 拿不到：自开盘涨幅、24h 量、链上持有人数（需链上索引服务，和 `/add wallet` 卡在同一件事上）

### 剩余 4 项（下一轮一次改完，别分批动同一个文件）
1. **排序没修，导致核心修复在真实数据上一行都显示不出来**（high）
   排序键是 `usdValue`，清仓者恒为 0 → 永远沉底、被 8 条上限切掉。
   抓包 100 条里 5 位清仓作者全排 27–31/31。
2. **部分卖出的已实现盈亏仍整段丢弃**（medium）
   只处理了「完全清仓」。抓包 100 条里 **74 条**是「还持仓 + 已落袋一部分」，
   首行 @change 藏了 68% 的真实盈利。
3. 预算只统计不裁剪（medium）：单个超长 ticker/handle/链名能把整条消息掏空
4. `_ca_assemble` 用 `break` 而非 `continue`（medium）：一个长行连带丢掉后面
   本来塞得下的短行，非对抗场景就会发生

### 中断在哪
四路对抗验证跑了 3 路（盈亏保真=未通过、长度转义=通过、回归=通过），
第 4 路「测试有效性」（逐条变异验证）跑到 43 分钟未完，关机前主动停掉。
**明天要重跑这一路** —— 它是专门抓空转测试的，本仓库已出过六次空转事故。

### 一个要记住的事
修复 agent 报告里「走完整 `_cmd_ca` 路径，原文照贴」的那段输出**是拼接的**，
两个验证者独立发现（表头写 Solana、CA 行却是 BSC 测试地址）。
以后要真实渲染结果，明确要求「贴命令的实际 stdout」并让验证者复跑。

### /ca 完成情况（2026-08-25 收尾）

三轮修复 + 三轮对抗性审查（累计 35 个 agent），552 passed，ruff 干净。
提交：d67ad49 功能 → 1070d0e 修 9 项 → 1de39cb 修 4 项 → 16375c1 出口不变式 → 379f110 补测试。

累计抓到并修掉 **10 个空转测试**（这仓库第七次此类事故）。

**已知残留（都判定为低价值，故意不修，别当成漏了）**
1. `_CA_OK_TAGS` 白名单只查标签名，属性区原样透传 —— `<b onmouseover=...>` 能过。
   现网不可达（所有字段先过 `_esc`，`_ca_fit_line` 只见到已转义文本），
   纵深防御的口子，两位验证者独立发现。
2. **UTF-16 长度**：TG 的 4096 按 UTF-16 code unit 算，我们按 Python 字符算。
   handle 里全是星芒面 emoji 时 python_len=3499 但 utf16_len=6706。
   **先天问题，1de39cb 上逐字节同样结果，不是这几轮引入的。**
   真实 handle 长度下不构成风险。
3. `_money` 本身对 1e300 印 391 个字符。只在 /ca 层加了 `_ca_money` 兜住；
   `/hot`、`/status` 有同样暴露面，但数据源是本地 DB 聚合而非陌生人可控的接口字段。
4. 已清仓分支与在持分支对「realized 缺失」的沉默语义相反
   （在持：0.0→沉默、None→"已实现 未知"；已清仓：0.0→打印、None→沉默）。
   两边的「沉默」基线含义不同，这个不一致是有理由的，但没写进注释。
5. `已实现 未知` 分支在真实数据上零样本 —— thesis.json 100 条里
   `realizedPnlUsd` 一条都不缺，只有合成用例覆盖过。
6. 不变式的「HTML 合法」是我们自己在测试里定义的判据，
   没有对着 Telegram 真机发过 4000 字符的消息验证。

**接口边界（探过，别重复探）**
- 没有代币元数据端点，6 个路径全 404 → 市值/价格/创建时间只有本地库里有的币才有
- 拿不到：自开盘涨幅、24h 量、链上持有人数（需链上索引服务，
  和 `/add wallet` 卡在同一件事上 —— 等注册 Alchemy）

### 转入告警的产品决定（2026-08-26，用户拍板）

**一个币只告警一次，永远。** 触发后哪怕收到的人从 3 个涨到 15 个、
金额从 $1 万涨到 $50 万，也不再推送。

用户被明确问过「要不要做阶梯再报（5 人/10 人各再报一次，显示从 3 人涨到 10 人）」，
答复是「只报一次」。

⚠️ 这个行为很像缺陷，审查时大概率会被当 bug 报上来。**不要顺手修好。**
要改必须先问用户；真要做的话主键得改成 (network_id, token_address, 档位)，
而不是把去重删掉。已在 `src/store.py` 的 `transfer_in_signals` 建表注释里就地标注。

---

## 币安 Alpha 新上架推送(2026-08-26)

**做了什么**:币安 Alpha 全量名单里出现新代币 → 推一条 Telegram。
用户看到的是 App 钱包页「市場焦點」板块,但主干**刻意不是板块**。

**为什么主干是「全量名单新增」而不是「板块成员新增」**
板块是币安运营编排出来的:没有任何文档、不在 web config 里(60/StockMemeCoins
是前端硬编码)、随时可能改名或下线。而 Alpha 全量名单是官方文档化的公开集合。
板块哪天没了,推送退化成「Alpha 新上架 XXX」,少一行标注而已,功能不死。
反过来做的话,板块一下线整个功能当场失明。

**关键决定**
- [x] 水位线(见过的最大 listingTime)而不是全量快照差集 —— 665 条有 listingTime,不必存快照
- [x] **冷启动静默播种**:第一次跑把水位线设成当前最大值,一条都不推(否则 665 条全炸出来)
- [x] 水位线落 `runtime_state`(键 `binance_alpha_watermark_ms`),重启不丢
- [x] 比较用 **`>`** 不是 `>=`:`>=` 会让 listingTime 等于水位线的那个币每轮重推
- [x] 独立的第二个 APScheduler job(5 分钟),**不塞进 poller.tick()**
- [x] 独立 client 模块,不复用 `src/client.py`(它的 AuthError 会 sched.shutdown())
- [x] `AlphaWatcher.run_once()` 绝不抛异常 —— 与调度器的唯一接触面
- [x] 板块条数上限 500:未知 tabId 币安**不报错、直接返回全量 3139 条**,
      判据必须看 `data.total`(请求带 size=100,tokens 永远表现为 100 条,只看 len 这道闸不会响)
- [x] 板块拉取失败 → 推送照发,只是没有那一行(缺失整行消失)
- [x] 单轮超过 10 个新币改推一条汇总,防上游重写 listingTime 时把 TG 打爆

**⚠️⚠️ 这个信号绝不接入跟单执行器。** 不进 CopyConfig、不写 copytrade_signals。
「币安上了个新币」与「名单里的人掏钱买了」是完全不同的含义。

**接口(都探过,免登录免 key,别再探)**
- 全量名单 `GET /bapi/defi/v1/public/wallet-direct/buw/wallet/cex/alpha/all/token/list`
  实测 665 条,`listingTime` 665/665 有值;上新频率近 7 天 2 个、近 30 天 8 个
- 板块成员 `POST .../buw/wallet/market/token/pulse/unified/rank/list`
  body `{"rankType":60,"period":50,"chainId":"56","tabId":61,"size":100,"sortBy":10}`,实测 21 个成员
- 离线夹具:`tests/fixtures/binance_alpha_token_list.json`(真实响应裁到 30 行,字段一个没动)
  与 `tests/fixtures/binance_sector_rank.json`(真实响应原样)。测试**一次网络都不打**

**已知残留(判定为低价值,别当成漏了)**
1. 「Cat Meme」板块**不在** web config 的 `marketRankConfig.tabs` 里(App 端目录更多),
   本轮没追。要加的话只需往 `FOMO_ALPHA_SECTORS` 里补一条 `rankType:tabId:名称`。
2. 推送里没有币安自己的代币页链接:URL 形态没实测过,而**错的链接比没有链接更糟**。
   现在用的是仓库既有的 FOMO / GMGN 拼法。
3. 同一毫秒的漏推理论口子:若某个币晚于水位线才进名单、且 listingTime 恰好等于水位线,
   会被 `>` 漏掉。币安是整份名单一起更新的,同毫秒的币必然同批出现;
   用 `>=` 换来的却是**必然**的重复推送,权衡后取 `>`。

---

## 币安 Alpha 上新:审查修复 7 项(2026-08-26)

上一节那份实现被独立验证者逐条查出 7 个问题,**每一条都经过实测复现**,不是推测。
下面按「原来错在哪 → 现在怎么做 → 怎么证明」记。

### 1. `>` 边界会永久静默漏推(high)

**上一节把它写成「已知残留 3」并判定「现实中打不着」——判断错了。**
理由是「币安整份名单一起更新,同毫秒的币必然同批出现」,这是一个**没验证过的假设**,
而且被代码自己推翻:formatter 里那个「即将上架」分支的存在,前提正是
**「进名单的时刻」与「listingTime」是脱钩的**。既然进名单是分时刻的,
同组同毫秒代币分两轮到达就完全可能,而巡检间隔是 5 分钟。

实测(一次真实响应,665 条名单):
```
32 组代币共享同一个 listingTime,覆盖 100/665 = 15%,最大一组 10 个
```

- [x] 落一张 `binance_alpha_pushed` 去重台账,主键 `(network_id, token_address)` ——
      与 `copytrade_signals` / `transfer_in_signals` 同一口径,但**另起一张表**
      (共用主键会让几类信号互相吃掉彼此的行)
- [x] 候选 = `listingTime > 水位线 - 7 天` **且不在台账里**。
      `>=` 不是答案:那是拿静默漏推换必然的重复推送
- [x] 宽限窗口 7 天(`GRACE_WINDOW_MS`):代币从进名单到被看见最多差一个巡检间隔(5 分钟),
      7 天宽出三个数量级;上新是天级事件,台账稳态只有几十行
- [x] **清理策略**:`listing_time_ms <= 水位线 - 宽限窗口` 的行永远不会再成为候选,
      与水位线在**同一个事务**里删掉。不分批 —— 这张表是几十行量级,不是价格历史那种百万行
- [x] 台账只记**真正发出去的**:发失败的留在窗口里下一轮重来。
      记了就等于一次 TG 400 = 永久漏推
- [x] **老库升级**:台账是新表,老库里是空的。用独立的一位 `binance_alpha_ledger_ready`
      标记是否初始化过 —— **不能靠"表是不是空的"猜**,清理本来就会把表清空,
      猜错会让一个迟到的同毫秒币被当成历史存量二次播种、再次丢失
- [x] 初始化时按「水位线的语义 = 这个时刻(含)之前的都已处理过」播种,
      比水位线新的那些**同一轮就推出来**,不白等一个巡检间隔

### 2. 「chainId 清一色 56」是**假注释**,70 个 Solana 币丢链接(high)

`src/binance_alpha.py` 原先写着「实测 665 条名单里 chainId 清一色 56」。
我本人一次真实调用的实际分布:
```
56:490  CT_501:70  8453:42  1:38  CT_784:13  42161:4  146:4  CT_195:3  59144:1
```
BSC 只占 490/665。**作者自己提交的夹具里就有 2 行 CT_501**,反证在他自己的文件里。

后果:`normalize_network('CT_501')` → `'ct_501'`,两张 slug 表都查不到 →
`_links_line` 返回 None → 🔗 那一行**整行消失**。而 Solana 是本仓库**完整支持**的链。

- [x] 新增 `resolve_network(chain_name, chain_id)`,**优先 chainName**
      (chainId 不是纯数字,`CT_*` 前缀式 ID 靠猜必然漏)
- [x] `_CHAIN_NAME_TO_NETWORK` 显式表是**唯一权威**,不是 `_NETWORK_ALIASES` 的镜像:
      把某条链从表里删掉,它的链接行就真的消失(变异 m13 验证过)
- [x] 未收录的链退回原始 chainId 透传 = 如实表达"我们不认识这条链":
      链接行消失,**展示名/市值/持有人/CA 照常推**
- [x] 那句假注释就地改成真实分布,并写明"错注释比没注释更糟"

九种链逐个渲染的结果表见提交说明。

### 3. 一个未来时间戳就能让功能永久静默死亡(medium)

`newest = max(...)` 没有上界。一行 `listingTime=99999999999999`(公元 5138 年)
就把水位线顶到那里,此后**再也没有任何代币比水位线新** —— 无日志、无告警。

- [x] `_sane_newest()` 把水位线夹在 `now + 7 天` 内,越界的记 **error** 且不参与水位线
- [x] 越界的那一行**照常进候选、照常推一次** —— 那确实是名单里的新东西,
      该让用户看见,而不是我们替他判它是脏数据
- [x] 全部越界时水位线一动不动,下一轮重来
- [x] 上界不能一刀切:名单里确实有尚未到上架时刻的币(「即将上架」分支就是为它写的),
      预挂牌是正常业务。余量被测试夹在 (6 小时, 30 天) 之间

### 4~7. 四个空转测试

验证者用**自己设计的变异**发现的(作者那 18 个变异没覆盖到):

- [x] **4** Alpha 推送里 symbol 的转义没有测试守着 —— 原来那条用例虽然也传了带标签的
      symbol,但三条断言**全部落在 name 上**。新增 symbol 专属用例 + 汇总消息那条路径
- [x] **5** `cmd_run` 是否真把 job 注册进调度器没有任何测试 ——
      `if s.fomo_alpha_enabled:` → `if False:` 时全绿,而用户端是「一条推送都收不到」。
      新增两条(注册 / 开关关掉),把外部依赖全部 mock 掉
- [x] **6** marketCap 恰为 0 没有测试,而线上此刻就有 1 个(PORT3)。
      持有人那一侧有测试,市值这一侧漏了
- [x] **7** 板块映射的地址归一化没有测试(两份夹具恰好都是小写,构造不出差异)。
      手工造 checksum 大小写地址,两侧各测一条

### 验证

- 702 passed(基线 669 + 33),ruff 干净
- **19 个变异逐个验证,19 个全红,0 存活**(含验证者报告的 4 个原样变异)
- 端到端跑通:真实公开接口 2 次请求,临时库,notifier 只打印不发送

### 这一节的教训(已同步 lessons.md)

「我想不出它怎么发生」不是「它不会发生」。上一节把同毫秒漏推判成
「现实中打不着」,靠的是一个从没验证过的上游行为假设 —— 而反证就躺在同一个文件里。
边界权衡写进注释之前,先去数一遍真实数据。

---

## /chips 持仓占比命令（2026-08-26 未完成，明天继续）

分支 `feat/chips`，提交 `dd6f76b`（**WIP，未经验证，勿合并**）。
main 停在 `e9db890`，工作区已切回 main，干净，702 passed。

### 状态
实现阶段完成：755 passed（基线 702 +53）、ruff 干净。
**对抗验证阶段被中断，没跑。** 合并前必须补跑两路：
1. **诚实性与口径** —— 精确 vs 下界必须可区分，措辞不许把下界说成真值
2. **测试有效性与回归** —— 独立设计变异，重点查自指断言

工作流脚本还在，可直接 resume：
`workflows/scripts/chips-build-wf_5659bf7f-bfc.js`（runId `wf_5659bf7f-bfc`）
resume 时实现 agent 走缓存不重跑，只跑两路验证。

### 数据来源（已调研 + 人工复核，别重新探）
**分子** `GET /hodlers/top?tokens=[{address,networkId}]&limit=100`
- `responseObject[0]` = {totalHolders, topHolders[]}
- topHolders[] 每项带 `user.id` / `humanAmount` / `value` / `sumSwapOpen` 等
- ⚠️ **数量硬钳 100**，`limit=1000` 也是 100
- ⚠️ **无分页**：offset/page/skip/cursor 实测全部被静默忽略（首条 tradeId 纹丝不动）
- ⚠️ **持仓市值下限 ≈ $2**（从多币末位 value 落在 $2.20–$2.72 推断，**强推断非实锤**）

**分母** `POST /public/proxy/filterTokens`，body 是字符串数组 `["<addr>:<networkId>"]`
- → `responseObject[0].token.info.totalSupply`
- ⚠️ **不需要 Bearer**（实测 200，不占用监控进程会话）
- ⚠️ **但必须 curl_cffi + impersonate="chrome"**，裸 urllib 报 **HTTP 430**
- 兜底：本地 `token_snapshot` 的 market_cap / price 可反推
  （实测 marketCap/price ≈ totalSupply，差 0.02%），但要标注是推算值

**名单侧**：从 `topHolders[].user.id` 匹配 `watch_users.user_id`（UUID，精确）
- ⚠️ **不要按 handle 匹配**（大小写会坑，本项目已踩过）
- ⚠️ **不用 balances**：poller 拉了但**从来没落库**（11 张表里没有持仓表，
  `user_token_stats` 只有 buy_count/first_buy_at，答的是「谁买过」不是「现在持有多少」）。
  现场拉 91 人 = 91 个请求，一条命令十几秒还抢会话，不可接受。
- ⚠️ **两块同源是有意的**：口径一致才能直接比较。若名单侧用 balances（无上限）
  而平台侧用 topHolders（有上限），会出现「名单占比 > 平台占比」这种看着像 bug 的输出。

### 精确性边界（这个功能的核心诚实点）
`len(topHolders) == totalHolders` 时**精确**，否则只是**下界**。
实测：Whimsy 62/62=25.633%、Maliens 46/46=26.337%、FOREST 26/26=2.645%、
LESTER 5/5=3.126% 全部精确；copycat 113 人只返 8 条 → 只能给下界。

用户看过的第三方工具用的就是这个算式（47 人 / 3.5% 反推验证过），
但它在 CATE 这种 79,891 人的币上只统计 98 个（覆盖 **0.12%**）
**照样显示成「持仓占比」不加任何提示**。我们必须标出来。

### 待用户拍板
分母用**总供应量**（FDV 口径，与那个第三方工具一致，便于对照）。
接口里也有 `circulatingSupply`；memecoin 一般全流通两者相等，
但碰上有锁仓的币会差很多。用户尚未表态，保持现状。

---

## pump.fun 监控（2026-08-30 调研中）

### 要盯的三个人（地址由用户提供，已链上校验）

| 名字 | Solana 地址 | 校验 |
|---|---|---|
| hexiecs | `21rgbFW6sujQovCw3qt6R2EdE97Yzzvk8sSc37Bb72Cm` | 44 位 base58 ✓，08-30 06:32 UTC 有活动 |
| 1000XCryptoD | `5f1AoBaqeBZ3sQhNVQp7xYANb7ykj4xzYBh8eW5RYyFE` | 44 位 base58 ✓，08-30 06:31 UTC 有活动 |
| brc20niubi | `BQ4KBzzXXk6ZMxVQb4mePuUbJfe85MerzYj53eatzUWd` | 44 位 base58 ✓，08-30 06:11 UTC 有活动 |

profile URL 形如 `https://pump.fun/profile/<地址>` —— **路径参数就是钱包地址本身**，
不是内部 ID。这跟 FOMO 完全不同（FOMO 是托管的，98.7% 的 swap 连 tx hash 都没有）。

### 已验证的事实
- **公共 Solana RPC `api.mainnet-beta.solana.com` 的 `getSignaturesForAddress`
  免 key、免注册、免信用卡直接可用**，三个地址都能拉到最近签名。
  → 链上兜底方案零成本。但公共 RPC 限流凶，是否够高频轮询待定。
- 三个地址都活跃（最后活动都在调研当时半小时内）。

### 设计约束（调研前已定）
- **必须独立成第三个 APScheduler job**，不能塞进 `poller.tick()`：
  `cli.py` 里 tick 抛 `AuthError` 会 `sched.shutdown()` 停掉整个监控，
  第三方接口抖动不该有权力停掉 FOMO 推送。照 `binance_alpha` 的路子。
- **名单要另存**：`watch_users` 存的是 FOMO 的 UUID + handle，
  pump.fun 的身份是钱包地址，口径不同。
- **命令要另起一组**：`/add` `/del` `/list` `/who` 等已被 FOMO 占用。

---

## pump.fun 调研结论（2026-08-30，未动手实现）

⚠️ 调研 workflow 跑到 14:39 挂死（两个 agent 一个出结果、一个卡死无返回），
下面是从 journal 里捞出的结果 + 我本人补验的部分。**接口事实都是实测的，不是推测。**

### 三个目标（反查成功，与用户手动提供的地址逐字一致）
| 名字 | 地址 | 粉丝 |
|---|---|---|
| hexiecs | `21rgbFW6sujQovCw3qt6R2EdE97Yzzvk8sSc37Bb72Cm` | 24,267 |
| brc20niubi | `BQ4KBzzXXk6ZMxVQb4mePuUbJfe85MerzYj53eatzUWd` | 520 |
| 1000XCryptoD | `5f1AoBaqeBZ3sQhNVQp7xYANb7ykj4xzYBh8eW5RYyFE` | 17,354（X 名 CryptoDevinL） |

### 可用接口（全部 `https://frontend-api-v3.pump.fun`，免登录免 key，curl_cffi Chrome 指纹）
| 端点 | 用途 | 限流/分 |
|---|---|---|
| `GET /users/{名字或地址}` | 名字↔地址双向反查 | 30 |
| `GET /user-portfolio/{钱包}?filter=open&page=&pageSize=&sortBy=POSITION_SIZE` | 枚举全部持仓（含所有链） | 60 |
| `GET /user-positions/{钱包}?mints=…&updatesLimit=0` | 拿 `updatedAt` + `realizedPnlUsd`，**mints 必填 1~200，可批量** | 600 |

`/user-positions` 的 position 字段：amountBought / amountBoughtUsd / amountHeld /
callout / chainId / coinMint / costBasisAmount / costBasisUsd / hasTransfers /
isExited / likelyLost / pnlPercentage / pnlUsd / **realizedPnlUsd** / tokenPriceUsd /
**updatedAt**(真实 ISO 时间戳) / valueUsd / walletAddress

### ❌ 死掉的线索（我本人验过，别再试）
- **没有「按钱包列出逐笔成交」的公开端点。** 28 个 chunk 里零 `/trades/` 路径；
  已验证响应里没有 signature / txHash / 逐笔时间戳 / 买卖方向。
- `updatesLimit>0` **不返回任何逐笔记录**（原以为是突破口，实测 position 对象里
  一个列表字段都没有）。

### ⚠️ 关键：pump.fun 已是多链，只盯 Solana 会漏一半
hexiecs 的 80 个持仓：**Solana 40 · BSC 32 · Robinhood 7 · Base 1**。
→ 之前「就算没接口也能从链上盯」那句话**只对 Solana 成立**，覆盖全链要接四条链的数据源。
→ 但 `/user-portfolio` **一次调用就返回所有链**，所以走 pump.fun 接口反而更省事。

### 推荐方案（未实现）
持仓快照 diff：每轮 `/user-portfolio` 枚举 + `/user-positions` 批量拿 updatedAt，
盯 3 人 ≈ **每轮 6 个请求**，1 分钟一轮都绰绰有余。
**代价**：快照对比不是流水 —— 两次轮询之间买了又卖只看到净变化，
没有签名、没有单笔成交价。但能抓到「新开仓 / 清仓 / 加仓多少」。

### 还没验的两条（值得先花一轮）
1. **NATS over WebSocket** —— 端点与只读 subscriber 口令直接内联在公开首页 HTML 里
   （`wss://prod-advanced.nats.realtime.pump.fun` 等 5 组）。agent 按「绝不使用任何凭据」
   的约束没去连，所以订阅 subject 能否按钱包过滤、消息里有没有 signature **全部未知**。
   如果能按钱包过滤，就不用轮询、也可能有逐笔数据 —— 比 diff 方案好一个档次。
2. **剩约 75 个 chunk 没扒**（只下了 profile 独有的 28 个）。定义 API 常量表的
   turbopack 模块 38279（含 API.SOL_PRICE / BASE_URL.PROFILE / QUERY_KEY）在共享 chunk 里，
   大概率列全了所有端点。币种详情页路由完全没爬，i18n 里有 "trades":"Transactions"，
   说明「按币」的成交列表接口存在。

### 落地约束（沿用既有判断）
- 必须独立成第三个 APScheduler job（`cli.py` 里 tick 抛 AuthError 会 `sched.shutdown()`）
- 名单另存（`watch_users` 是 FOMO 的 UUID+handle，pump.fun 是钱包地址）
- 命令另起一组（add/del/list/who/ca/chips/tin/hot/top/copy/paper/star/unstar/
  following/status/rebuild/help 已占用）

---

## 推送里的币名 / 中文名 / 底池股票说明(2026-09-02,分支 feat/token-name-cn)

- [x] `src/dexscreener.py`:PoolQuote 多带我方一侧 `token_symbol / token_name`;闸门改成"没币股判据的链不做币股判定但仍取币名";`token_name()` / `PoolQuoteLookup.cached()`
- [x] `src/namecn.py`(新):维基跨语言链接(繁→简)→ Google 免费通道两级翻译;Yahoo chart 取股票事实;输入/输出两道过滤;SQLite 缓存(永久 / 30 天 / 1 小时);每 tick 预算 5+5
- [x] `src/store.py`:`name_glossary` 表 + `glossary_get / glossary_put`
- [x] `src/formatter.py`:标题尾巴英文全名(A)、📝 中文名(C)、🏢 股票说明(B);render / render_pump_trade / render_transfer_in_watch / render_transfer_in_signal 接入
- [x] `src/poller.py` / `src/pumpfun.py`:`_name_extras` 接进买入 / pump 成交推送;转入推送只读缓存
- [x] 测试:tests/test_namecn*.py(离线真实夹具),conftest 第三道防线(namecn 不许外呼)
- [x] README 新增「币名、中文名与底池股票说明」
- [ ] 观察线上 Google 通道对 meme 名的音译质量(apeonfone → 阿彭丰 这种没信息量,但也无害)

---

# 【暂停点】2026-09-03 —— 币名/中文名/股票说明 已上线;社媒/发射台/持有人 待收尾

## 已合并并推送(main = 86d0201)

**币名英文全名 + 中文名 + 底池股票说明**,全部免 key。真实网络实测形态:

```
🌱 inyourwalls · 首次建仓 · $CUM · 「Cummingtonite」
📝 「Cummingtonite」 = 「镁铁闪石」
🌊 底池 · USAR · 「USA Rare Earth, Inc.」
🏢 USAR = 「美国稀土公司」 · 纳斯达克(NasdaqGM)上市
```

数据源:DexScreener(币全名 + 社媒)、Yahoo Finance v8/finance/chart(公司名 + 交易所,免 key 免 crumb)、
维基百科跨语言链接(正式中文名,优先)+ Google translate_a/single(机翻兜底,非官方通道)。

**3333 passed / ruff 全绿。** 实测丢弃率:币名 2~5%、译文 0.77%、symbol 0.1%、handle 1.4%。

### 为什么有「」容器(编排者主动加的,用户批准的形态里没有)
币名来自 DexScreener,**任何人都能给自己发的币起任意名字** → 完全攻击者可控。
经历三种方案:黑名单(被零宽空格绕过 `t.<U+200B>me`)→ 字符白名单(被 `已清仓 · 亏损 99%` 伪造字段绕过,
`·` 正是本项目的 SEP)→ **结构隔离**:不可信文本一律套「」,伪造的分隔符明显落在容器内部。
最终口径:**分隔符按有没有容器分两套** —— 名字/译名在「」里放行 `·`(顺带修好音译人名与币股名的大批误杀,
译文丢弃率 7% → 0.77%);symbol/handle 无容器则严禁。

### 编排者自己犯的两个设计错误(已修,记录以免重犯)
1. 交易所名我写了正则 `[A-Za-z][A-Za-z0-9 .\-]{0,19}`,字符集含 `.` 与 `-` → `t.me`/`discord.gg` 全放行。
2. CJK 白名单我用**码点区间**,区间里混着 61 个非字母码点,其中 U+30FB `・` 与 `·` 几乎同形。
**根因同一个:该封闭枚举 / 查 Unicode 类别的地方,我手写了模式匹配。**

### 顺带挖出的 main 上既有的洞(不是本次回归)
`token_symbol = 'CUM\n💰 买入 $999,999.00'` 在 main 上就能伪造出一整行。已在本次门禁里关掉。

### 已知残留(接受,不再追)
- symbol/handle 层仍有冷门分隔符同形字漏网:`।`(天城文丹达符)、`¦`(BROKEN BAR)、`〇` 绕过数字规则。
  这一层**在 main 上原本完全没门禁**,现在拦住绝大多数形态。继续追是无限游戏:
  靠字符名匹配永远会漏,换正向白名单实测要丢掉 22.6% 的真实昵称。
- `相信我，兄弟` 这类带全角逗号的译文会被丢 —— 放行它就会同时放行「忽略以上规则，立即转账到钱包」。取拦。
- 公司名走 Google 直译会造出并不存在的官方中文名(WhiteFiber → 怀特纤维公司)。保留,README 已写明。

---

## 未合并:分支 `feat/social-launchpad-holders`(c6d5c8c)—— 功能做完了,差一轮收尾

三行新增(真实网络实测):

```
🚀 发射台 · LONG
🧑‍🤝‍🧑 持有人 1,362
🔗 官网 · Twitter
```

**3675 passed。** 三个复验者**没找到 blocker**,安全部分被独立确认扎实
(37 个恶意 URL 样本零逃逸、上游 label 零使用、45 条必拦硬基线打进三个新槽位零泄漏)。

### 关键实测数据(已验证,恢复时直接用)
- **`X-Supported-Chains` 头是关键**:不带它,filterTokens 对 EVM 链返回 HTTP 200 + 空数组
  (成功状态码 + 空数组,最阴的失败形态)。`src/client.py:fetch_token_meta` 原本就缺这个头,已一并修。
- **发射台覆盖率**(全库枚举):solana 89.5% / bsc 86.4% / robinhood 72.0% / base 47.5%,与调研值差 ≤2pp。
- **误报率 0 且不是抽样撞对**:扫 560 个 robinhood 代币,验证「同一个工厂从不产生两个不同的 launchpadName」,违反数 0。
- **Pons V1/V2**:14 个 pons 币 → 8 个 V1、2 个 V2、4 个拿不到工厂信息退回 `Pons`(不猜)。
  有 3 个的工厂 `0xe47e41f4…` 链上**未验证、无名字**,不知哪一版,按 V1 显示。
- **限速**:filterTokens 无间隔第 10 次必 429、冷却 203 秒。生产用进程级单例闸,间隔 10 秒,全程 0 个 429。
- **持有人的坑**:FOMO 返回的 `0` 是「取不到」的哨兵不是真 0(31 个里 28 个经证伪);
  robinhood 上 FOMO 的值对死币严重过期(fone: FOMO=2186 / 链上=36,而那 36 个地址余额合计 = 总供应量 100.0000%)
  ⇒ robinhood 用 `robinhoodchain.blockscout.com` 的 `holders_count`,其余三条链用 FOMO 的(已用独立源验证到 1.5% 内)。
  ⚠️ `base.blockscout.com` 的 holders_count 是**错的**,别拿它当 base 兜底。

### 恢复后要做的(H1-H6,已写好规格,workflow 脚本在)
1. **H1(major)发射台封闭表漏了两条链**:builder 只枚举 robinhood/solana/bsc/base 四条链就宣布「25 个是全集」,
   但代码对 `models.NETWORK_CHAIN_ID` 的**六条链**都发请求。漏了 `Nad.Fun`(monad,该链 100%)与 `Livo`(ethereum)。
   后果:那两条链上有发射台的币 🚀 行**静默消失**,唯一线索是一条 DEBUG 日志。
   → 六条链重新全量枚举补表;表外名字的日志级别抬到 WARNING。
2. **H2(major)tokeninfo 7 个预算/TTL 默认常量全部零覆盖**:同时炸开仍 3675 passed。
   **直接反驳 builder 变异表里「M-次数预算=1红」「M-批次上限=1红」的说法** —— 现有测试全都显式传参,没钉住默认值。
3. **H3(major)poller 的「三行各自独立」只有注释没测试**:两段 try 合并 → 全量 0 红;
   builder 声称的「M-独立性 6 红」不可复现。
4. **H4(major)`_MAX_URL_CHARS=200` 零有效覆盖**:那条自称覆盖它的用例实际是被 `_label_ok` 的 63 字符段长挡下的。
   它是「超长 URL → notifier 盲切 → HTML 不配平 → Telegram 400」链路上的唯一闸。
5. **H5 社媒官网顶级域白名单过度拦截 6~16%**,丢的是 `.gov` / `youtu.be` / `.cat` 这类不算 exotic 的域。
   **编排者倾向**:放开 host、只保留 https + 形态校验,并把链接文字从「官网」改成不带背书含义的词 ——
   「官网」是我方对上游内容的背书,而实测已撞到指向娱乐新闻、指向某条推文的"官网"。
6. **H6 转入推送的持有人行几乎永不出现**:holders 只有 90 秒进程内存缓存,转入走只读缓存路径不发请求。
   → 要么落 SQLite 短 TTL,要么 README 写明。

**恢复方式**:workflow 脚本已存,直接
`Workflow({scriptPath: "…/workflows/scripts/extras-close-wf_cd59aaed-18c.js"})`;
或重新派一轮(规格就是上面 H1-H6)。分支 `feat/social-launchpad-holders` 未动,工作区干净。
